# ItsJustCAD — Phases Remaining

Status as of 2026-09-01. Shipped milestones (M2–M5 core, layers, gumball,
undo, dims/sheets/PDF, DXF/glTF/OBJ/STL, local-LLM onboarding, per-doc chat
persistence, native menus, clipboard) are omitted — this tracks what's LEFT.

> Interop note (2026-09-04): DXF **block export** was a gap — `Geometry::Instance` used to export zero entities, so block instances vanished on save while the importer already read `BLOCK`/`INSERT`. Now closed: the exporter emits a `BLOCKS` section + `INSERT` entities and blocks round-trip (parametric instances bake to static blocks). See `docs/interop.md`.

## Design system (report batches A–F) — COMPLETE ✅

HIG-grounded design revision, run as sequential batches. All six landed.

- [x] **A — Tokens + a11y foundation** — `surface_elevated`/`on_surface_tertiary`/`destructive` roles, WCAG contrast helper + passing ≥4.5:1 body-role tests, type scale (caption/title/weight), spacing guards. *(already implemented; verified)*
- [x] **B — Iconography** — Lucide clean, de-dup guard, `Line→pen-line`; added `Paperclip` icon replacing 📎. *(shipped ce0d1e1)*
- [x] **C — Menus + keyboard** — Window menu, shortcuts shown, disable-don't-hide, stateful View menu (Panel Hide/Show ⌘\, display/lighting radios). *(shipped f470de7)*
- [x] **D — Component correctness** — button roles (destructive/pressed/"…"), segmented controls + toolbar grouping, unsaved-changes + neutral-title alerts. *(already implemented; verified)*
- [x] **E — Ergonomics + states** — empty-doc viewport hint + add-layer toolbar to top (rest already present: empty chat, hit-target floor, focus ring, middle-truncate, striped rows). *(shipped 36e6418)*
- [x] **F — Polish** — killed dead `HISTORY_H` (history height already token-relative), wired `reduce_motion` to spinners, corrected download-cancel warning (partial `.part` is kept for resume; autosuggest accent fill already present). *(shipped)*

## Queued — small, high-impact

- [x] **Pending deck usability** — download-that-can't-run (disk-space gate + unknown-RAM warning; RAM gate was already in), download wired to active deck even with Model Setup closed (per-frame terminal poll), model-mismatch surfaced for Anthropic too + suffix-tolerant matching (`qwen3` ↔ `qwen3:latest`), active model always visible in the chat header, download failures announced in the deck transcript. *(shipped; builds on 5d16875)*
- [ ] **Local model finetune** — 0.6B too weak; default 4B Qwen; free synthetic training data from the command registry.
- [x] **Chat encryption** *(deferred, now shipped)* — chat-session files (`~/.config/itsjustcad/chats/<uuid>.json`) encrypted at rest with AES-256-GCM, keyed by a per-user 256-bit data key in the OS keychain (`keyring` crate). Transparent auto-unlock (no prompt). Self-describing `ICEC` magic-header container (magic ‖ version ‖ 96-bit nonce ‖ ct); legacy plaintext stores load and upgrade to encrypted on next save. Plaintext fallback with a one-time warning when no keychain (headless/CI/bare Linux) — never a data loss or hard fail. Key store is an injectable `KeyStore` trait so tests never touch the real keychain. See `crates/app/src/chat_crypto.rs`. **User-controllable, opt-in**: `chatencryption on|off` (alias `encryptchats`) toggles at-rest encryption, persisted to ui.json, **default OFF** — the app isn't code-signed, so an always-on default would prompt every user for keychain access on first chat (revisit once M-sign lands). Turning it on/off is lossless either way (self-describing format loads both). The per-process key cache means at most one keychain prompt per launch when enabled.

## Core UX debt

- [x] **M-layerspanel** — Rhino-style 7-column layers table + lock/linetype. *(verified shipped)*
- [x] **M-uilayout** — Rhino docking layout. *(mostly landed; verified)*
- [x] **M-legacyskin / M-uipolish** — onboarding-driven skins (AutoCAD/Rhino/Revit theme + fonts + aliases), app icon, toolbar iconography. *(shipped: skin token-sets + per-skin alias tables + `expand_alias` pre-parse wiring + template-picker choice were already live from batches A–F. This cart added the residual: a live `skin native|autocad|rhino|revit` app-verb (classify + execute_line, staged-apply via `pending_skin`, headless-acknowledged, persisted to ui.json); localized skin names + a language picker in onboarding (i18n keys `skin.*`/`onboard.skin.prompt`, en+es); tests for skin code round-trip, ui.json persistence, alias additivity/non-shadowing.)* **Skin + Language are now menu-selectable** (Theme menu, native + in-window): radio groups Native/AutoCAD/Rhino/Revit and English/Español, current one checked (from live `CadOrigin`/`Lang`), each firing `SetSkin`/`SetLanguage` through `apply_menu_action` (same apply+persist path as the `skin`/`language` verbs) — closes the discoverability gap where skin/language were only reachable via verb or onboarding. App icon: DONE — brand mark (tuxedo-cat) at 16–1024 PNG set + `.icns` in the bundle (`CFBundleIconFile`) + runtime window icon (`main.rs`); generator `examples/gen_icon.rs`. No platform sizes deferred for macOS. Toolbar iconography/ergonomics: covered by batches A–F (Lucide set), not re-touched. **Quit (⌘Q) + Close (⌘W) added to the menu bar** (fixes: macOS had NO reachable menu-bar Quit — it was `#[cfg(not(macos))]`-gated and the muda auto app menu carried none). Now: `MenuAction::Quit`/`Close` in File on every platform (⌘Q/⌘W), both routing through the dirty-doc guard → `PendingNav::Quit` → `ViewportCommand::Close` (single-window app: Close == Quit); macOS additionally gets a proper native app menu (About + native ⌘Q via `PredefinedMenuItem::quit`). i18n `menu.file.close` (en/es); native+in-window parity + i18n completeness tests green.

## Big domain adds

- [x] **Camera projections** — COMPLETE (audited 2026-09-02; core had already shipped). Perspective FOV via `camera <mm>` full-frame lens math, 2-point (`camera 2point`, projection shear — verticals stay vertical), equirect panorama + equidistant fisheye (cubemap capture + remap), phone-lens sims (iPhone/Pixel/Galaxy ultrawide/main/tele as 35mm equivalents). Gap closed this session: stateful **View ▸ Camera radios** (Perspective / Two-Point / Panorama 360° / Fisheye; checked from the live camera, fire the same `camera` verbs the deck uses).
- [x] **Render & colors** — COMPLETE (audited 2026-09-02; had already shipped). Pencil display mode + sketchy NPR edges, `sketchup` preset verb (sun shading + profile edges + sky/ground gradient), lighting rework (Working = hemispheric-diffuse default, Sun = SPA key light, Presentation = specular opt-in), per-object `color` + ColorMode (by-layer/by-object/by-type/random).
- [x] **M-enviro** — COMPLETE (closed 2026-09-02): `shadowstudy`, `sunhours` grid heatmap, `facesunhours` per-face insolation, EPW import (sets location + reports stats), `sunpath` yearly dome, `radiation` annual kWh/m²·yr per face (EPW DNI/DHI month×hour bins, occlusion-tested, bins embedded in the op-log). **Deck critique hooks closed this session**: every analysis now stores a compact structured `AnalysisReport` on the document (min/avg/max, 6-bin distribution, worst/best sample locations with compass facings — token-frugal, regenerated on replay); new read-only `report [analysis]` registry command serves it to the deck; system prompt gained an "Environmental critique" section teaching analysis → report → design feedback (sun-hour thresholds, radiation hot/cold faces, shadow coverage) so the LLM grounds critique in sampled locations ("north face at (0,10,2) gets 0.5 h — don't put the terrace there").
- [x] **Diffusion render** — COMPLETE (2026-09-02). Control-image export (`controlimages <prefix>` → depth/edge/mask PNGs from the current view) + cassette-style SD backends speaking their real protocols (ComfyUI graph/upload/history-poll, A1111/Forge/Draw Things `img2img`+ControlNet, Replicate cloud predictions) + the end-to-end `render <prompt…>` GUI flow: capture control images from the active view → async job on the tokio runtime → result PNG saved to the private runtime dir and shown in a floating "AI Render" window. `render cancel` really aborts (task drop); setup surface is `render backends` / `render use <name>` / `render test` (reachability probe with "cannot reach X — is it running?" guidance). All errors land on the command line, never silent. Per-protocol mock-HTTP-server tests; headless warns-and-ignores. Ships with NO backend active — backends are user-configured in `render_decks.json`.
- [x] **M-structural** — COMPLETE (audited + closed 2026-09-02). Audit: nearly all had ALREADY shipped — verbs (grid/story/beam/column/slab/wall/load/support), **IFC-structural export** (typed IfcBeam/IfcColumn/IfcSlab/IfcWall with swept-solid bodies + full IfcStructuralAnalysisModel: IfcStructuralCurveMember/SurfaceMember, IfcStructuralPointConnection with boundary conditions, IfcStructuralLoadGroup with point/linear/planar actions), **SAF export**, and **M-bim-import** (IFC import reconstructs typed Frame/Area members with stories/materials via `import_semantic`; mesh fallback for the rest). Closed this session: SAF upgraded from a nonstandard ZIP-of-CSV to a **genuine SAF 2.2.0 .xlsx workbook** (hand-rolled OOXML on the existing store-mode ZIP writer — no new deps; 9 SAF sheets, numeric cells native, `.saf`/`.xlsx` both accepted, export dialog fixed from a dead "xml" filter), plus the **never-analyze disclaimer** embedded in both formats (xlsx docProps description + IFC FILE_NAME header note: "geometry+topology handoff — no analysis results"). Interop only — the app never claims to analyze.
- [x] **M-expressive** — COMPLETE (audited + closed 2026-09-02). Audit: the shared engine ALREADY existed (`kernel-mesh/src/formfind.rs`) and funicular/tensegrity/cablenet already routed through the one `dynamic_relaxation`; nothing needed re-unifying. Closed this session: engine hardening (per-node point loads, true Barnes/Day kinetic damping option, Schek force-density solver — deterministic Gauss–Seidel) + hard tests (settled sag vs analytic catenary ≤2%, kinetic == viscous equilibria, pinned-bar stability, perturbed-start uniqueness, net symmetry), and **Frei Otto minimal surfaces shipped** — `minsurf <closed-curve-sel> [n]` (aliases soapfilm/minimalsurface) stretches a soap film (zero-rest-length force-density harmonic net) across any closed boundary curve; undo/replay-stable, deck-callable. Deliberately left analytic (closed-form is exact; relaxation adds nothing): hypar, geodesic, spaceframe, gaussvault (exact catenary section), gridshell (rides analytic surfaces).

## New phases (added 2026-09-02)

- [x] **M-landscape** — core landscape design tools SHIPPED 2026-09-03 (aa47c7b…fe315a6; pure math in `commands/src/landscape.rs`, all verbs registry/GBNF/deck-callable, undo + replay-stable, analytic-terrain tested):
  - *Contours OUT* DONE: `contours <interval> [major-every]` — marching-triangles extraction from the terrain mesh, chained polylines on 'contours'/'contours-major'. NOT done: contour labels, sheet/PDF feed.
  - *Grading & earthwork* DONE: `pad <x,y> <w> <d> <elev> [slope]` (flat pad + side slopes to daylight, first pad snapshots pre-grading heights) and `cutfill` (cut/fill m³ TIN prism estimate, heights embedded in the logged op, AnalysisReport → `report cutfill`). NOT done: freeform `grade` regions, retaining-edge detection.
  - *Planting* DONE: embedded 12-species catalog (`assets/plants.json`, real data), `plant <species> <pt> [age-years]` (age-scaled trunk+canopy mesh draped on terrain, layer 'planting', occludes sun analyses automatically), `plantrow <a> <b> <spacing>`, `plantschedule <path.csv>` + AnalysisReport. NOT done: `plantmass <region>`, seasonal deciduous transparency.
  - *Water & drainage* DONE (advisory-only wording throughout): `flowarrows [n]` steepest-descent glyphs + `ponding` local-minima markers, both with AnalysisReports. NOT done: watershed accumulation, `swale`.
  - *Hardscape* DONE: `sitepath <curve-sel> <width>` ribbon draped on terrain with 1:12 accessible-slope warning. NOT done: stairs/ramp generators, site sections through terrain.

- [x] **M-deckagent** — deck LLM interactivity + agent harness (closed 2026-09-02):
  - *Terse mode* DONE: `TERSE_STYLE_HELP` + `terse_adjusted()` (hard `TERSE_MAX_TOKENS` cap plumbed into the openai_compat/anthropic wire bodies); per-cassette `DeckConfig.terse` override, default ON for local OpenAI-compat, OFF for cloud; LLM ▸ Terse Replies checkable toggle (in-window + native), persisted in decks.json.
  - *Clarify-before-act* DONE: `QUESTION: <text>` message form (`agent::parse_question`), CLARIFY_HELP in the full prompt + condensed rule in the brief local prompt, accent-bordered Question entry in the transcript; user's next message continues the turn. Mixed commands+QUESTION fails safe to plain chat.
  - *Plan-execute harness* DONE: `agent::{Plan, PlanStep, parse_plan}` + `tool_loop::run_plan_loop` (Plan/Question step decisions, MAX_STEP_ATTEMPTS per-step retry, mid-run replan, cancel-preserves-state, round budget); streaming path mirrored in deck_pane (checklist Entry::Plan ticks live, step-per-turn auto-continuation, verify turn with bbox/schedule, plan persisted in the per-doc draft → resumes after relaunch, Stop pauses). GBNF root now `draft | question | plan`.
  - NOT done (deliberate cuts): `run_plan_loop` is not yet the live execution path (the streaming deck_pane driver is; the loop is the future FOSS-harness core and is fully mock-tested); no dedicated per-step GBNF "STEP" form (steps are plain draft blocks — the existing fence grammar covers them).

- [x] **M-chatmd** — rich markdown in the deck chat transcript (closed 2026-09-02):
  - *Rendering* via `egui_commonmark` 0.24 (MIT/Apache, matches workspace egui 0.35): deck bubbles + the streaming bubble render **tables as striped grids** (the headline — pairs with the `report` verb for analysis stats/schedules), plus bold/italic, inline + fenced code, lists, headings, rules. Chosen over a hand-rolled parser because it slots into the existing bubble frames without touching the custom Question/Plan/Status entry styles (base integration 618f561).
  - *Prompt nudge*: `TABLE_HELP` "## Presenting data" section in the full system prompt (pipe tables for object lists/stats/schedules) + a "tables ARE terse" rule in `TERSE_STYLE_HELP`; brief local prompt untouched (GBNF-constrained draft/question/plan forms — tables come from cloud models and `report`).
  - *Copy*: right-click "Copy message" on User/Deck/Question/Plan bubbles copies the RAW markdown source (`entry_copy_text`), so pasted tables stay tables.
  - Tests: prompt-nudge + terse-interaction tests, `entry_copy_text` unit test, `chat_markdown_table` kittest preview snapshots (dark + light).

- [x] **M-deckprompt** — unified compact verb catalog + on-demand help (closed 2026-09-08). BUG FIX: the deck chose its system prompt by `is_local_url(base_url)`, and the claude-code cassette has an EMPTY base_url — `is_local_url("")` is `true` — so Sonnet was misclassified as a weak local model and handed the ~10-verb *brief* prompt, blinding it to geodesic/hypar/funicular/tensegrity/minsurf + ~90 other registry verbs (it told users "no geodesic primitive / no form-finding").
  - *Compact catalog*: retired the brief-vs-full fork. New `prompt::compact_system_prompt` (used by claude-code, Anthropic, AND local models) lists EVERY registry verb as name + one-line summary, grouped by the `Category` taxonomy, derived from `registry()` so it can never drift. Catalog ~11 KB / whole compact prompt ~27 KB vs the full prompt's ~67 KB. The full prompt is now an explicit opt-in fallback (`ITSJUSTCAD_FULL_PROMPT`), never url-gated. `select_prompt(full_opt_in) -> PromptChoice` is a pure, unit-tested selector.
  - *`help <verb>` as a deck tool*: un-denylisted `help` (moved from `DECK_APP_VERB_DENYLIST` to `CANONICAL_DECK_APP_VERBS`); the compact prompt teaches "you see names + one-liners — emit `help <name>` for full syntax/args/examples before an unfamiliar verb." `handle_extract_events` runs `help <verb>` in-app (read-only) and stashes `help_lines` output; the turn finisher threads it back as a follow-up user message so the syntax reaches the model mid-turn (bounded by the retry budget). GBNF grammar unchanged (already covers the full verb set); terse + `/no_think` still key on the local heuristic.
  - Tests: `compact_catalog_lists_every_registry_command_name` (anti-regression: geodesic/hypar/… present), `compact_prompt_is_smaller_than_full_yet_complete`, `select_prompt_defaults_to_compact_regardless_of_backend` (the bug pinned), `deck_help_verb_is_not_an_app_verb_and_captures_syntax_to_thread_back`, app-verb partition/advertise tests updated for help.

- [x] **M-dyntabs** — dynamic Blocks + Plugins right-dock tabs (closed 2026-09-03). AUDIT CORRECTION: the premised "dynamic sheets-tab pattern" did not exist — sheets are doc-side only (`sheet`/`sheetview`/`print`, no UI tab anywhere in crates/app); the real precedent generalized was the FIXED 3-tab right-dock strip (`tabstrip::PanelTab`).
  - *Tab registry*: `PanelTab::FIXED` + dynamic `Blocks`/`Plugins` variants; `TabState::visible_tabs(has_blocks, has_plugins)` (content OR user-pin OR active keeps a dynamic tab; empty+inactive un-pins so it disappears; reappears with content), `sync_dynamic` reconciled per frame; `strip_ui` draws a caller-supplied list so dynamic tabs share the exact folder-tab look/collapse/width contract. Note: Blocks is de-facto always visible — `Session::default` seeds the 14 starter pblocks, which ARE definitions.
  - *Blocks tab*: rows from pure `dyntabs::block_rows` (name, live instance count via plain-name/`source` match, geometry count, pblock `name=default` signature; `__param/` bakes filtered). Insert prefills `insert <name> ` (placement by typing), library section lists `blocklib` (cached, ↻ re-scan) with per-row `blockload`, Delete fires the NEW guarded `blockdelete` verb — op-logged substrate command refusing while instances exist (button pre-disabled with count tooltip; confirm step otherwise), undo restores plain+parametric entries, replay-stable.
  - *Plugins tab*: rows from `dyntabs::plugin_rows` (usage, summary, pretty JSON). Run prefills the verb, JSON opens a read-only monospace popup, Reload/Delete ride the existing `plugin reload|delete` verbs, Open-folder reveals `~/.config/itsjustcad/plugins/`.
  - *Deck integration*: UI-plane grammar grew `panel chat|sessions|layers|blocks|plugins` (`UiAction::PanelTab`, transient — applied then stripped from ui.json so a relaunch never re-forces a tab); `UI_VERB_HELP` advertises it; both plane-completeness tests extended. `ITSJUSTCAD_PANEL_TAB` accepts the new names.
  - Both tabs are VIEW + verb-trigger surfaces only — every mutation goes through the one substrate/command-line path; op-log/undo/replay untouched.
  - NOT done (deliberate cuts): no per-tab close button or reorder (the fixed tabs have neither — dynamic tabs disappear by emptiness instead); no kittest snapshot of the new tabs (registry logic + derivations are pure-tested; snapshot infra would need seeded doc state for marginal value).

## Performance (planned 2026-09-02)

- [~] **M-perf** — STEP 1 SHIPPED 2026-09-03 (d6290ab, 6bd87e3); step 2 (GPU) deferred by design. Parallelize hot loops with rayon (MIT/Apache, deterministic, replay-safe), GPU compute only if profiling still hurts after:
  - [x] *Step 1 — rayon on analysis loops* (closed 2026-09-03): `sunhours` grid ray casts, `facesunhours` + `radiation` per-face scoring, `shadowstudy` time frames, raw-LAS point decode, binary-STL triangle decode — all via shared per-item kernels with ordered `collect` (parallel output bit-identical to the `_seq` references kept for tests; no parallel float reductions). Determinism tests per site (par == seq + repeat-run) plus an `#[ignore]` bench (`bench_analysis_200_boxes`). Bench on a 200-tower scene (12-core Intel, quick profile): sunhours 469→85 ms (5.5×), radiation 369→58 ms (6.3×), facesunhours 68→16 ms (4.2×), shadowstudy 19→14 ms (1.4×).
  - Step-1 deliberate skips: `minsurf`/`force_density` is Gauss–Seidel (reads already-updated neighbours — inherently sequential); `dynamic_relaxation` accumulates link forces by scatter with order-dependent float sums (a parallel gather would reassociate additions and change replayed results); LAZ decompression and the E57 reader are sequential streams; OBJ/glTF/Collada parsers are stateful (index interning/XML) — not worth the fiddle.
  - Keep ordered `collect` so output order == sequential (replay invariant); no rayon inside op-log mutation paths — analysis + import + solver inner loops only.
  - *Step 2 — wgpu compute (only if needed)*: occlusion ray casting as compute shader. Costs buffer plumbing + float-determinism care vs replay (analysis bins in op-log shield replay; keep CPU path as reference + test oracle). Deferred until step-1 profiling shows a real remaining hotspot.

## Planting — tropical/subtropical packs (planned 2026-09-03)

- [x] **M-plants-tropical** — extend the plant catalog beyond the temperate 12 (closed 2026-09-03). Catalog 12→33 (9 Caribbean, 6 Valle del Cauca, 6 Guayaquil), all real mature size/canopy/growth/deciduous; `climate_zones`+`native_regions`+`layer` tags (serde-default so legacy files still load); palms render crown-on-trunk (form "palm", 0.85 bare-trunk fraction) for low-sun shadows; `ClimateBand::from_latitude` derives Köppen band; `plant` prints a climate advisory (not an error) when a species is out of the doc's band; new `plantcatalog [region|zone]` query lists filtered species; new `miyawaki <closed-region> [density]` grows a seeded (SplitMix64 from region hash + salt, byte-stable replay) stratified native mini-forest at 3–5 saplings/m², reports species-mix %/stem count/area, warns on too-few natives, and carries the self-thinning advisory. Tests: back-compat load, Köppen bands (Guayaquil/Cali/Miami/Boston), suitability, palm-vs-tree shape, plantcatalog filtering, miyawaki determinism/count/natives/strata + replay.
  - *Schema*: species gain `climate_zones` (Köppen: Af/Am/Aw tropical, Cfa/Cwa subtropical...) + `native_regions` tags; existing 12 tagged temperate. Backward-compatible serde (untagged = temperate).
  - *Caribbean pack*: royal palm (Roystonea regia), coconut palm, ceiba (kapok), flamboyán (Delonix regia), tabebuia/roble amarillo, sea grape (Coccoloba uvifera), mango, guayacán (Guaiacum), almendro (Terminalia catappa).
  - *Valle del Cauca / Cali pack*: samán (Samanea saman — the iconic pasture giant, 25m canopy), guadua bamboo (Guadua angustifolia — clumping, structural), chiminango (Pithecellobium dulce), gualanday (Jacaranda caucana), carbonero, cachimbo (Erythrina poeppigiana).
  - *Guayaquil / coastal Ecuador pack*: guayacán (Handroanthus chrysanthus — the yellow bloom), algarrobo (Prosopis), palo santo (Bursera graveolens), ceibo (Ceiba trichistandra — bottle trunk), neem (naturalized), fernán sánchez (Triplaris cumingiana).
  - Real mature sizes/canopy/growth rates per species; palms get a dedicated canopy shape (crown-on-trunk column, not ellipsoid) — matters for shadow studies at low tropical sun angles.
  - *Climate awareness*: `location` verb already georeferences the doc — derive Köppen band from latitude (rough: |lat|<10° tropical, 10–23.5° tropical/monsoon, 23.5–35° subtropical); `plant` warns when species climate mismatches doc location ("Betula in Guayaquil — advisory: outside climate range"); `plantcatalog [region|zone]` lists filtered.
  - Deciduous flag stays (dry-season deciduous: flamboyán/ceibo drop leaves — affects seasonal shadow transparency when that lands).
  - *Miyawaki method*: `miyawaki <region-sel> [density]` — dense native mini-forest generator: picks ONLY species native to the doc's region/climate from the catalog, stratifies into 4 layers (canopy / tree / sub-tree / shrub — catalog gains a `layer` field), plants 3–5 saplings/m² in seeded-random mixed placement (never rows, neighbors differ — seed from op data so replay is stable), all at sapling age. Report: species mix %, stem count, area. Warns if catalog lacks enough native layered species for the location ("Miyawaki needs natives — N found"). Advisory line: survival thinning is part of the method (~30–50% self-thins; densities are intentional).
  - Data-driven JSON only — no code per species; packs user-extendable + LLM-authorable like plugins.

- [x] **M-plantsymbols** — 2D top-view planting symbols so plan drawings read as proper planting plans, not 3D canopies-from-above (closed 2026-09-03). Pure deterministic glyph geometry in `commands/src/landscape.rs`: `plan_symbol_segments(style, center, canopy_d)` → line-segment soup scaled to canopy diameter, lying flat on the ground; `PlanSymbol` enum with 5 standard drafting styles — **round** (24-gon circle + 8 radiating branch stubs = shade tree), **conifer** (8-point spiky star), **palm** (8 radiating frond spokes, the iconic palm glyph), **shrub** (dotted/stipple ring, also the fallback for fastigiate columns), **clump** (7 dot-crosses = bamboo/guadua). Catalog gains optional `plan_symbol` field (serde-default; derived from `form` when absent — guadua explicitly tagged `"clump"` since it is a "column" form but drafts as a cluster). `plant_object_symbol(name, positions)` is the single bridge: recovers center (XY centroid) + canopy diameter (2× max horizontal radius) + ground-z from a `plant:<id>` mesh, so every consumer draws the identical symbol at the actual planted (age-scaled) size.
  - *Where drawn*: **PDF + SVG plan exports** overlay the symbol on the 3D feature edges, gated to the **Top (plan) projection** only (a plan glyph is meaningless in elevation/iso) — SVG is always Top so always includes them, PDF gates per sheet-view direction; symbols honor the plant's layer color via the existing per-layer grouping / lineweight path. **Live viewport + pencil/NPR + headless render** overlay symbols via `scene::append_plant_symbols` behind a new `plantsymbols [on|off]` (alias `plansymbols`) toggle — 3D mesh always stays for perspective/sun analysis; symbols are added lines on top. Toggle is a deck-advertised app-verb (`VIEW_VERB_HELP`, canonical list) and forces a GPU re-upload when flipped.
  - Tests: per-style geometry counts + planar extent, linear canopy scaling, determinism, mesh-recovery of center/ground-z, legacy-catalog back-compat (no `plan_symbol` → derived from form); SVG top view emits ≥32 symbol lines on the planting layer; PDF top view draws substantially more than the same plant in front view (proves plan-gating); `append_plant_symbols` adds exactly the oak's 32 lines and is a no-op without plants; `plantsymbols` classify parsing + deck advertise/deny audit.
  - NOT done: seasonal deciduous stipple density, per-species custom symbol art (all styles are procedural), symbol in DXF export.

## GUI + usability testing (planned 2026-09-02)

- [x] **M-guitest** — automated GUI tests + automated usability audits (closed 2026-09-03):
  - [x] *Journey tests* (kittest AccessKit queries + synthetic typing/keys against the real off-screen App; GPU-`#[ignore]`d like the preview suite, `cargo test -p itsjustcad journey_ -- --ignored`): box→select→undo (asserts select is unlogged so undo reverts geometry), layer add + `layerlock` (row visible via AccessKit), autosuggest partial→popup→Tab-accept, Blocks tab lists a user `block` definition (starter param blocks keep the tab up from frame one), chat QUESTION bubble + PLAN `[x]/[ ]` checklist render (CPU kittest, runs in the normal suite).
    - Journeys found + fixed two real bugs: Tab-accept in the command line was DEAD (egui consumed Tab for focus traversal first; input now `lock_focus`es only while the popup is open) and the A+/A− text-size steppers were ~19 px tall (raised to the hit-target floor).
    - Skipped: onboarding flow + gumball drag (need real pointer capture over the 3D viewport, which previews skip headless), import/export dialogs (native rfd dialogs — not in the egui tree), Model Setup download (mock-server plumbing outweighs the assert; download.rs logic is unit-tested).
  - [x] *Snapshot suite* — pre-existed (app_dark/light, layers, dialog, panel/chat toggles, 6 chat states × 2 themes; goldens in `crates/app/tests/snapshots/`, refresh via `UPDATE_SNAPSHOTS=1 … -- --ignored`); regenerated for the A± fix. Display-mode chips not snapshotted separately: the chips live over the 3D viewport pass that headless previews skip.
  - [x] *Usability audits as tests*: AccessKit-tree walk asserts every interactive widget ≥ `Spacing::HIT_TARGET` height; keyboard-only Tab traversal never dead-ends and cycles back to the start; native-menu accelerators unique per action per style (same action twice on one shortcut allowed — Settings/Model Setup on ⌘,); reduce_motion busy indicator is static (no repaint loop) while the spinner demonstrably loops. WCAG ≥4.5:1 for both theme palettes already covered in theme.rs (`mode_palettes_meet_wcag_aa` + viewport-tag/status tests). Disabled-don't-hide already covered by menu.rs `selection_edit_items_disabled_when_empty`.
  - [x] *Headless `--shot` theme*: already honors `ITSJUSTCAD_THEME=dark|light` (headless.rs `resolve_theme` + env round-trip tests) — the "hardcodes dark" gap was closed earlier. Full GUI-chrome capture from `--shot` deliberately NOT added: it would drag the eframe/kittest test harness into the shipping binary; the kittest preview tests already produce full-chrome PNGs in both themes.
  - Snapshot/GPU tests stay `#[ignore]`d (Metal-only) while logic journeys (chat render, reduce-motion, shortcut audit) run everywhere; human usability testing stays manual.

## intemfit — lot subdivision (planned 2026-09-03 — NOT started; plan only)

- [x] **M-intemfit — COMPLETE (Phases 1–12, closed 2026-09-05)** — parametric site planning (roads → blocks → lots), CityEngine/TestFit-style, as a native ItsJustCAD Rust feature (target decided 2026-09-03: NOT a Rhino plugin, NOT a JSON macro). Full build plan in **`docs/intemfit-plan.md`** (grounded in Manuel's questionnaire — ask before inventing requirements). **All 12 phases SHIPPED (1–7 2026-09-04, 8–12 2026-09-05)** (Phase 3 = first shippable milestone — send to Manuel; Felkel skeleton shipped as convex-exact partial, default stays offset):
  - **Phase 1** — new pure-Rust `crates/subdivision` (zero egui/doc/commands deps): `Polygon2d` (CCW/shoelace/point-in-poly), `OrientedBox` (min-area rect via rotating calipers on the monotone-chain hull; rotated-rectangle tested), `PolylineTools` (resample/Douglas-Peucker/perpendicular/arc-length), `split_by_line` (Sutherland-Hodgman half-plane clip — all Phase 3 needs), `clip_bridge` (sole i_overlay touch-point, int-scale ×1000). **i_overlay finding: 8.1.0 is pure Rust, MIT OR Apache-2.0 (AGPLv3-clean), ships BOTH boolean overlay AND polygon offset (OutlineOffset) — offset blocker CLEARED, no geo/Clipper2 fallback.** 10 §8 sample blocks (JSON); #11/#12/#13 = Phase 5b.
  - **Phase 2** — `lotsubdivide` + `lotsettings` verbs (registry/GBNF/deck-callable), sticky `SubdivisionSettings` on the Document (serde-default, logged/replay-stable). Bridge in `commands/src/lot.rs`; perimeter/streetfollowing error cleanly (Phase 4/7). Preview overlay skipped (bake-on-run, acceptable); undo via CreatedOnLayer.
  - **Phase 3** — recursive-OBB `method=grid` (short-axis split on long-axis midpoint, recurse while area>min; street-access/snap/edge-align/seed modifiers). **Deterministic for a fixed seed** (byte-identical replay). All §8 assertions pass on all 10 blocks. ~50 new tests, clippy 0.
  - **Phase 4** — offset/perimeter `method=perimeter` (`subdivision/src/subdivision/offset_sub.rs`): inward-offset by `offset_width` (i_overlay via clip_bridge) → interior core; sample the boundary at `lot_area_min/offset_width` (≥ width) jittered by irregularity (splitmix64 seeded from block-hash+seed, salted apart from grid → byte-identical replay); cut orthogonal-to-boundary and keep each wedge minus core as a perimeter lot; `subdivide_core` on → recursive_obb the core, off → one hollow-ring lot. **Degenerate fallbacks verified**: offset≈0 → recursive OBB (byte-identical); huge offset (interior collapses) → clean fallback no panic; thin rect (#1) self-collapses → fallback; plus a covered-area safety net. All §8 assertions pass on the 10 blocks (area conserved, no overlaps, real rings, deterministic). `lot.rs` wires `method=perimeter` (deferral removed) with the grid bake path (logged op, written-back ids, undo, replay). 8 new §8 integration tests + 6 unit tests; clippy 0.
  - **Phase 5 SHIPPED 2026-09-04** — road-network generation + block extraction + street tagging (`lotgeneratesite`). New `crates/subdivision/src/streets/` (`street_graph.rs` = StreetGraph of centerlines + widths + hierarchy tier + adjacency) with the FOUR rectilinear generators in `generators/`: **orthogonal** (recursive min-area-OBB split of the site to ~2× block depth, a spine centerline per split), **skewed** (same, split dirs globally rotated 22.5°), **organic** (a sinusoidally-deviated spine down the long axis + perpendicular connectors spaced by block depth), **culdesac** (a long-axis spine + alternating perpendicular stubs ending in turnaround bulb loops). `block_extractor.rs` offsets each centerline by ROW/2 into a ribbon (`clip_bridge::offset`/hand-rolled two-side ribbon), boolean-subtracts each ribbon from the site (`clip_bridge::difference`, subtracted one-by-one so disjoint ribbons don't merge and swallow interior blocks), and produces **tagged blocks**. `blocks/block.rs` + `block_edge.rs`: **every `BlockEdge` carries `{is_street, street_id, street_width, street_length, is_alley}`** (the §5 hard dependency) — an edge coincident with a street ROW (½-width from that centerline, and not on the untouched site boundary) is tagged with the street's id/width/length; original-boundary non-street edges are `is_street=false`. **Alley tier**: with `loading=AlleyLoaded`, each block is bisected along its long axis into two sub-blocks whose shared edge is tagged `is_alley` at `alley_width`. Verb `lotgeneratesite [sel] <pattern> roadwidth= blockdepth= alleys=on|off seed=` bakes roads→`roads` layer, blocks→`blocks` layer (one logged op, road-ids-then-block-ids written back, undo removes both, replay byte-identical, seeded-deterministic); registered in registry (deck-callable, GBNF), parsed/dispatched/exec. `StreetPattern`+`road_width`+`block_depth` added to `SubdivisionSettings` (serde-default). radial/hex/Voronoi error cleanly (Phase 5b). Confirmed a generated block feeds `lotsubdivide` and its tagged frontage lets a downstream recursive-OBB honour `force_street_access`. Tests: 6 integration (`tests/streets.rs` — 4 generators on rect + L-shape, non-overlap, coverage=site−ROW, **2×2 grid tagging correctness**, alley on/off, determinism) + 3 unit (street_graph/block_extractor) + 4 exec (bake/undo/replay-byte-identical/radial-error) + 4 bridge (`lot.rs`); clippy 0.
  - **Phase 5b SHIPPED 2026-09-04** — the three NON-rectilinear street generators (owner scope), each emitting a `StreetGraph` into the SAME `block_extractor`/tagging path as Phase 5. **radial** (`generators/radial.rs`): own polar layout — a center (site centroid; N centers along the long axis for large sites) → concentric ring roads (Connector) at block-depth spacing + radial spokes (Spine), clipped to the site into inside runs; blocks are annular sectors; no OBB site-split. **hexagonal** (`hexagonal.rs`): pointy-top hex lattice sized so each cell ≈ one block deep (circumradius = depth/√3) over the padded bbox, every deduped hex-cell edge = one centerline; blocks are the hex cells (boundary cells clipped). **voronoi** (`voronoi.rs`): jittered-grid seeds (splitmix64 from a quantized site hash + seed, salted → replay-stable) → Voronoi diagram as the **dual of `kernel_mesh::triangulate`** (circumcenters of the two triangles sharing a Delaunay edge = a Voronoi edge; hull-edge rays dropped, site clips the rest). `kernel-mesh` added as a subdivision dep — glam+serde-only, no subdivision dep → **no cycle, no new external dependency** (workspace crate). All three wired into `generate()` + `lotgeneratesite` (`parse_pattern`/`generate_site` clean-error stubs replaced; radial|circular|ring, hexagonal|hex, voronoi|voro accepted). Same bake path (roads/blocks layers, logged op, undo, seeded-deterministic → byte-identical replay); tagged blocks feed `lotsubdivide` honouring `force_street_access`. `block_extractor` gained a `0.03·block_depth²` crumb floor so the overlapping curved ribbons the polar/hex/Voronoi paths subtract can't leave pairwise-overlapping sub-cell slivers (rectilinear blocks ~block_depth², untouched). Sample blocks **#11 annular-sector / #12 hex-cell / #13 Voronoi-cell** added + run through §8 subdivision assertions. Tests: +6 `tests/streets.rs` (3 generators on rect+L, street-tagged, byte-identical replay, radial ring/spoke geometry, hex interior-cell size, Voronoi cell-count-tracks-seeds + **Delaunay-dual correctness on a known square seed set**, downstream force-street-access) + per-generator unit tests + updated `lot.rs`/`exec.rs` bridge tests; clippy 0.
  - **Phase 6 SHIPPED 2026-09-04 (on euro_latam placeholder defaults)** — lot rules in `crates/subdivision/src/subdivision/lot_rules/`, applied as a post-pass / mode on subdivision. **WidthMixSolver** (`width_mix.rs`): the packing problem — greedy fill weighted by running proportion deficit (pick the product with the largest target-share deficit that still fits; ties → narrower first, deterministic), then a swap pass that widens the LAST lot to absorb the remainder at the end (NOT smeared). Hits the requested proportions **within 5% on a 500 m frontage** (§9). **DepthController** (`depth.rs`): independent depth band (euro_latam 25 m ±5), clamp + in-band check, separate from area. **CornerLots** (`corner.rs`): interior-angle detection, widen the acute-corner lot by the bonus (euro_latam +15%), width auto-clamped to slack so no self-intersection. **FlagLots** (`flag.rs`): only when `allow_flag_lots`; pole width ≥ `flag_pole_width_min` (3 m); **pole area excluded from countable area** (§7.4 assumption — flagged to confirm). **LoadingStrategy** (`loading.rs`): FrontLoaded (full depth) vs AlleyLoaded (two-frontage, street→alley half depth; degrades to front when no `is_alley` edge present) off the Phase-5 block tags. **SliverMerger** (`sliver.rs`): repeatedly merge any lot below `sliver_area_frac × lot_area_min` (0.5) into its largest-shared-edge neighbour until none remain. A **`region` profile** (`euro_latam` default / `us_suburban` stub) + `LotWidthMix` on `SubdivisionSettings` (serde-default); euro_latam accessors (`effective_width_mix`/`effective_depth_target`/`effective_corner_bonus`) return `(value, used_placeholder)` so any run resolving a rule from the profile surfaces **"using euro_latam defaults (placeholder — confirm with Manuel)"**. Integrated in `lot.rs`/`exec.rs`: width-mix is opt-in (re-slices the frontage by packing when set — never forced on a plain grid run), then `apply_lot_rules` (corner + sliver) runs on every `lotsubdivide`; the message reports slivers merged / corners widened / width-mix ±% / the placeholder banner. New keys on `lotsettings` (region, loading, widthmix=6:0.25,8:0.5,10:0.25, depth+depth_tol, corner=15%+corner_angle, flag+flag_pole, mergeslivers+sliver_frac, alley_width) + a **`lotloading [sel] front|alley`** verb (sticky, logged, undoable). Tests: 30 subdivision unit (`lot_rules/*`) + 9 §8/§9 integration (`tests/lot_rules.rs` — width-mix 5% on 500 m, alley two-sided half depth, corner widen+clamp on acute block #10, flag exclusion, no slivers remain on §8 blocks + a Phase-5 generated block, Σ-area conserved, byte-identical replay, placeholder note) + 6 commands exec/parse (phase-6 keys, lotloading undo, width-mix report+placeholder, us_suburban no-placeholder, replay). Placeholders in use (swap when Manuel answers §1): width mix 6/8/10 m @ 25/50/25%, depth 25±5, corner +15%, alley 5 m, flag pole 3 m + pole-area exclusion. clippy 0.
  - **Phase 7 SHIPPED 2026-09-04** — skeleton / street-following subdivision (`method=streetfollowing`) — the perpendicular-to-curve method for cul-de-sac bulbs + curved streets. New `crates/subdivision/src/straight_skeleton/`: a `StraightSkeleton` **trait** + `offset_approx.rs` = **OffsetApproxSkeleton** (the Phase-7 approximate straight skeleton per §5). It partitions the block into one **face per contour edge** as the *nearest-edge* region — each face = the block clipped by the perpendicular bisector between that edge's supporting line and every other edge's; that seam is exactly what an inward offset wavefront carves, so it is the offset-approximate skeleton, computed in closed form per edge → robust, never panics on non-convex/notched blocks (coverage-checked with a centroid-fan fallback). `medial_axis_ridge` (iterated `clip_bridge::offset` insets) gives the ridge the unit tests assert (square → meets near centre; rectangle → medial ridge). **Felkel deferred to Phase 12** — the trait is left so it slots in behind the same interface. `subdivision/skeleton_sub.rs` runs the §7.5 steps: skeleton → faces; group adjacent faces along similar-curvature street runs into bands (turn <40°, uses `is_street` tags; every edge = frontage on an untagged block); corner regions by `CornerAlignment` (widest wins, tie-break length); **slice each band perpendicular to its street edge(s)** at `lot_width_min` — a closed-loop bulb band is **pie-sliced centroid→perimeter** (exact-tiling perpendicular wedges), an open curved band slices each convex face by shared global perpendicular cut lines so lot lines align across faces; merge lots < `lot_area_min`; merge shallow lots (`shallow_lot_frac`); `simplify`. All merges **area-conserving** (single-ring unions only, never drop the smaller piece); a `force_street_access` pass folds streetless wedges into a street neighbour. **Deterministic (pure geometry, no RNG) → replay byte-identical.** Wired into `lot.rs` via a shared `subdivide_by_method` (streetfollowing replaces the deferral; same bake path — logged op, written-back ids on `lots`, undo, replay-stable); width-mix no longer overrides the skeleton's own slicing. Tests: `straight_skeleton` unit (square faces meet-near-centre + 4 equal faces, rectangle medial ridge, notched no-panic+covers, triangle tiles) + `tests/skeleton_blocks.rs` on the §8 blocks that matter (#1 long-thin, #4 cul-de-sac bulb, #5 curved-street, #7 short street edge): Σ area == block within tol, no overlaps, no gaps (>95% coverage), every lot has street, **lots perpendicular to the street on bulb+curved (inward side ∥ local street normal within 30°, ≥60% of lots — the §9 visual claim as a structural proxy)**, sane lot count, byte-identical determinism + `lot.rs`/`exec.rs` bridge tests (runs/bakes/undo/replay). **Phase 8 (setbacks) picks up next.** clippy 0.
  - **Phase 8 SHIPPED 2026-09-05** — setbacks + buildable envelopes + frontage-at-setback. New `crates/subdivision/src/subdivision/setbacks.rs`: `classify_edges` tags each lot edge Front (street-tagged, or the longest edge on an untagged lot) / Rear (edge whose outward normal is most anti-parallel to the front) / Side (rest); `buildable_envelope` insets the lot per-edge by front/side/rear via a **per-edge half-plane clip** (`split_by_line`) — robust on non-convex/notched lots, and when setbacks exceed the lot the interior clip empties and it **collapses cleanly to `None`** (reported, never a panic). `build_to_line > 0` pins the front line to the build-to distance instead of `setback_front`. **Frontage measured at the setback line by DEFAULT** (§5, Manuel's ask): `frontage(at=setback|curb)` — setback = the envelope's front-edge extent (front setback line inside the lot), curb = the raw street edge; they differ on cul-de-sac bulbs / curves (inset arc shorter than the outer curb). Verbs (registry-registered → GBNF/deck): `lotsetbacks [sel] front= side= rear= buildto= envelope=on|off` bakes the envelope polygon per lot onto a **`setbacks` layer** (distinct from `lots`), one logged op, written-back ids, undo removes them, **byte-identical replay**; euro_latam setback numbers (front 3 / side 0 party-wall / rear 3 m) flagged "placeholder — confirm with Manuel". `lotfrontage [sel] at=setback|curb` reports frontage via the AnalysisReport / `report` plane (keyed `lotfrontage`) — read-only, never logged. Tests: 7 setbacks unit (rect area == (w−2·side)(d−front−rear); side=0 → full width; L/notch envelope inside + valid; collapse clean; build-to pins front; setback≠curb on a tapering lot; deterministic) + `tests/setbacks.rs` §8-block integration (irregular notch inside, bulb frontage setback≠curb, collapse on sliver/acute/notch) + `lot.rs` bridge (envelope area/collapse, frontage default) + `exec.rs` (bake/undo/replay, placeholder note, collapse-reported, envelope=off, frontage report + setback≠curb + not-logged). clippy 0. **Phase 9 (open space) picks up next; Phase 10 (buildings) is the explicit re-scope-with-Manuel gate.**
  - **Phase 9 SHIPPED 2026-09-05** — open space (feature placement + blind %-reserve). New `crates/subdivision/src/open_space/`: **feature placement** (default, Manuel's questionnaire) — `pocket_park` (rounded-rectangle green polygon, sized to `area` or a region fraction, intersection-clipped inside the region so it never crosses a street), `greenway` (linear trail corridor: a selected open polyline path or the region's long-axis spine, offset by width/2 into a closed simple ribbon; `area` sets width=area/length), `retention_pond` (exact-area regular n-gon basin footprint, clipped to region), `tree_save` (region outline preserved whole, or scaled about its centroid to `area` — natural shape kept). All return valid non-self-intersecting polygons (`is_valid_feature` guard). **Blind %-reserve** (owner opt-in keyword, §1) — `reserve_blocks` runs the deterministic street generator for the block set, scores each block `(area/max_area)·centrality` (biggest-AND-most-central), sorts with a quantised-centroid tiebreak, pulls whole blocks until reserved ≈ pct of the site; returns `reserved` (tagged) + `developable` (disjoint — what a later subdivide runs on). frac clamped `[0,0.9]` (whole-site = ruled-out no-subdivision mode); `reserve=0`/absent = off. **Verb `lotopenspace`** (registry → GBNF/deck): `lotopenspace [sel] type=park|greenway|pond|treesave [area=]` OR `lotopenspace [site-sel] reserve=<pct>`. Both bake onto the **`openspace` layer**, each object named `openspace:{park|greenway|pond|treesave|reserve}` so Phase 11 `lotreport` nets it out; one logged op, written-back ids, undo removes geometry+layer, **byte-identical replay** (feature + reserve). `open_space_reserve_frac` already in settings (serde-default 0). **Advisory on every run** — design intent, not hydrology/ecology engineering (pond = footprint not sized volume; tree-save = marked polygon not survey). Tests: 24 pure-crate (each feature valid+in-region+honours area; reserve≈20% near target + conservation reserved+developable==all blocks; reserved larger/more-central; developable excludes reserved & subdivides; reserve=0 off; deterministic same-seed) + 9 exec (bake on openspace layer, each type places a polygon, default=feature, undo removes, replay byte-identical, reserve pulls central blocks, reserve deterministic replay, reserve=0=feature mode, is_logged). clippy 0. **Phase 10 (buildings) is next — re-scope resolved in `docs/intemfit-phase10-scope.md`.**
  - **Phase 10 SHIPPED 2026-09-05** — buildings: footprints + stepped massing + roofs (owner scope, re-scope resolved in `docs/intemfit-phase10-scope.md`, folded into plan §7.6). New `crates/subdivision/src/buildings/`: `footprint.rs` (2D footprint inside the Phase-8 buildable envelope — 4 **fill modes** full/coverage/inset/typology + 4 **typologies** detached [compact centred mass], row/terrace [fills full lot width, party-wall side=0 medianería], courtyard [ring footprint with interior void], slab [large single mass]); `massing.rs` (extrude by floors, **height = floor_count × floor_height fixed**, floors ≥ `stepback_start_floor` inset cumulatively by `stepback_depth` → stepped massing; emits indexed-triangle `Mesh` with side walls + slab + top cap + courtyard inner walls; records **per-floor net areas → GFA = Σ**); `roof.rs` (flat/gable/hip/shed meshes on the top ring, pitch° drives peak rise; `PerTypology` → detached/row=gable, courtyard/slab=flat); `mod.rs` `build_on_envelope` orchestrates footprint→mass→roof into a `BuildingResult` (footprint poly + mass mesh + roof mesh + floors + GFA). Settings added (serde-default, euro_latam placeholders flagged): typology, footprint_mode, coverage_frac, footprint_inset, floor_height (~3 m), floor_count (2), stepback_start_floor, stepback_depth, roof_type, roof_pitch (30°). **Verb `lotbuilding [sel] typology= footprint= floors= floorheight= coverage= roof= pitch= stepback=`** (registry → GBNF/deck): per selected lot compute envelope (reuse setbacks) → footprint → stepped mass → roof; bake footprint (2D curve) + mass (3D mesh) + roof (3D mesh) named `building:{footprint,mass,roof}` onto the **`buildings` layer** (3 objects/lot), one logged op, written-back ids, undo removes, **byte-identical replay**, deterministic. Collapsed envelope skipped + reported (never a panic); euro_latam building defaults flagged "confirm with Manuel". `lotsettings` extended with sticky setback + building keys (front/side/rear/buildto/typology/footprint/floors/floorheight/coverage/roof/pitch/stepback) so they replay. Tests: 25 pure (footprint per typology inside envelope, all 4 modes incl. coverage≈frac×lot, height==floors×floor_height, step-back shrinks upper floors, GFA==Σ per-floor net areas < naive, each roof valid closed mesh at right height, full pipeline detached-gable/courtyard-void-flat, determinism, collapse→None) + 8 exec (bake footprint+mass+roof, undo, byte-identical replay, deterministic, euro_latam note, collapse reported, stepback lowers GFA, is-logged). clippy 0. **Phase 11 (yield) picks up: read built GFA (stored per-floor areas) for FAR = GFA / net site.**
  - **Phase 11 SHIPPED 2026-09-05** — yield reporting + option comparison. New `crates/subdivision/src/reporting.rs`: pure `YieldInputs` → `YieldReport::compute` (geometry-free summary math, unit-tested headless) — lot count + total/avg/min/max lot area, frontage stats (at the setback line §5), **GROSS vs NET developable area** (net = gross − open-space features − reserved blocks; a gross number lies once open space exists), open-space ratio, built **GFA** (Σ per-building), building count, **FAR = GFA / net** (+ FAR gross; `None` when net 0 so FAR is never a division-by-zero lie), lot-coverage % when footprints exist. `to_markdown` → GitHub Markdown table (M-chatmd renders in chat); `YieldComparison::diff`/`to_markdown` → A/B option-compare table (Δ lot count, net area, GFA, FAR, open-space ratio). **Verb `lotreport [site-sel]`** (registry → GBNF/deck): builds the report via bridge `lot::gather_yield_inputs` — lots on `lots` layer, `openspace:*` split into features vs `reserve`, GFA+footprint parsed off each `building:mass gfa=… fp=…` object name (`lotbuilding` now persists both onto the mass object), gross site = largest closed curve NOT on an intemfit output layer (else lots+open-space). Stored on new `Document::yield_reports` keyed `lotyield`, served by the existing `report` verb (`report`/`report lotyield`); **read-only, never logged** (like `lotfrontage`), deterministic. **`lotreport compare`** rotates the prior snapshot to `lotyield_prev` and diffs the two (minimal 2-slot A/B store). Empty/no-geometry → clean "nothing to report", never a panic. Deck prompt gains `YIELD_CRITIQUE_HELP` (judge FAR against NET, not gross). Tests: 11 pure `reporting` unit + 9 `lotreport` exec (lot count/area match baked lots within tol; net==gross no-open-space + ratio 0; net=gross−open when present; FAR=GFA/net on real buildings; served through `report`; not-logged + deterministic; empty-doc clean error; compare diff; compare needs two runs). clippy 0. **Only Phase 12 (polish) remains.**
  - **Phase 12 SHIPPED 2026-09-05 — M-intemfit COMPLETE (Phases 1–12).** Polish cart: (1) **Consistent indexing** (`crates/subdivision/src/indexing.rs`, `ConsistentIndexing`) — deterministic lot/block IDs by **spatial** order (row-major by block, then along-street), not creation order: `assign(&[Polygon2d])` buckets by centroid on an auto/fixed block grid, sorts buckets row-major (top→bottom, left→right), orders within a block along the principal axis then perpendicular, all with quantised deterministic tie-breaks → identical geometry gives identical indices regardless of input order; a re-run keeps every lot's number, adding one lot keeps a stable prefix. Wired into the bake — lots baked `lot #N`, blocks `block #N`. (2) **True straight skeleton — Felkel & Obdržálek (1998)** shipped as the **convex-exact PARTIAL** (honest, NOT default): `straight_skeleton/felkel.rs` (`FelkelSkeleton`) behind the existing `StraightSkeleton` trait runs the genuine priority-queue **edge-event** simulation on convex polygons (angle bisectors, speed 1/sin(θ/2), circular active ring, earliest convergent event per step → skeleton nodes; convex ⇒ no reflex ⇒ no split events ⇒ exact), and **falls back to `OffsetApproxSkeleton` on non-convex input** (split events not implemented — never ship a broken skeleton). Opt-in `lotsettings skeleton=felkel|offset` (`SkeletonImpl`, serde-default **Offset** for safety); `skeleton_sub` dispatches via the trait so nothing else changed. (3) **Optimization** deliberately **skipped** (plan §12 = optional; a speculative hot-path rewrite risked the replay-determinism invariant for no measured need). Tests: 7 indexing unit + 6 felkel unit + 9 `tests/felkel.rs` integration (square→centre node, rectangle→medial ridge, L→fallback covers, Felkel-vs-Offset agree on convex, no-panic on all §8 blocks, area/street-access/byte-identical determinism on the conservation-safe §8 set, default offset unchanged) + 4 exec (stable-index names are a 1..=n permutation, re-run identical indices, felkel setting bakes lots, unknown impl rejected). `cargo test -p subdivision` 162 lib + all integration green; clippy 0 new warnings. **Historical Phase-12 shape below.**
  - Remaining (Phase 12) [historical]: polish. Shape:
  - New pure-Rust `crates/subdivision` (no egui/doc deps): `Polygon2d` + `i_overlay` bridge (offset/boolean — the Clipper2 role; **Phase-1 blocker: confirm i_overlay offset quality + AGPLv3 fit**), `OrientedBox`, recursive-OBB / offset / straight-skeleton subdivision, street generators (orthogonal/skewed/organic/culdesac), lot rules (width-mix packing, depth, corner, flag, front/alley loading, sliver merge), setbacks + buildable envelope, open-space placement, footprints/roofs, yield reporting.
  - `lot*` verbs in crates/commands (deck-callable, GBNF, logged + replay-stable, seeded-deterministic); results bake to `lots`/`roads`/`blocks` layers; yield via the `report` plane; viewport preview before commit. NO Rhino toolbar/.rui/multi-target — those are dropped.
  - Phase order: 1 geom foundation → 2 verb+settings+preview → **3 recursive-OBB (first shippable, send to Manuel)** → 4 offset → 5 roads+block-extraction+street-tagging → 6 lot rules → 7 skeleton → 8 setbacks → 9 open space → 10 buildings → 11 yield → 12 polish. Do NOT attempt 10–12 in the first release.
  - Owner scope beyond Manuel (Hector, 2026-09-04): radial/circular + hexagonal + **Voronoi** street generators (Phase 5b — non-rectilinear; Voronoi = dual of our Bowyer-Watson Delaunay, no new dep) + a `loose` lot mode (irregularity unlocks past 0.4 to 1.0, routes to the organic subdivider) + opt-in blind %-open-space (`lotopenspace reserve=<pct>`, default off — feature placement stays default; reserved blocks netted out of yield). Still ruled out: whole-site no-subdivision mode. Open questions before Phase 5/6: width-mix product list, alley dims, "day one" blank (Q14), flag-lot area accounting — Phase-6 fallback defaults are metric **European/LatAm** (party-wall/medianería, deep solares, build-to street-wall; `euro_latam` region profile), NOT US suburban feet; every value a flagged placeholder until Manuel confirms.
  - Blockers to clear before coding: confirm i_overlay offset; obtain the referenced Python prototype (`/prototype/python`, not yet in repo) to port from; resolve Manuel's open questions.

## M-paperspace / M-annotation (SHIPPED 2026-09-08)

- [x] **M-paperspace / M-annotation** — SHIPPED: scale-independent PAPER-SPACE annotation layer on sheets. Mirrors the `SheetDim` pattern (paper mm coords on the `Sheet`, optionally anchored to a `SheetView`).
  - **Bug fixed**: model-space `Annotation::Text { pos, height }` has a model-unit height → the same text rendered huge at 1:50 / tiny at 1:500. Paper text now lives in paper space, sized in mm-on-paper.
  - **Types** (`crates/doc/src/sheet.rs`, all serde-default on `Sheet` so old files load): `SheetText { pos_mm, text, height_mm, view_index: Option<usize> }` (`Sheet::texts`); `SheetLeader { tip_mm, knee_mm, text_pos_mm, text, height_mm, view_index }` (`Sheet::leaders`); `SheetTag { pos_mm, text, shape: TagShape(Bubble|Square|Diamond), view_index }` (`Sheet::tags`).
  - **Verbs** (registry + parse + exec, deck-callable, GBNF auto-covered): `sheettext <sheet> <x,y> <text...> [height_mm]` (default 2.5 mm), `sheetleader <sheet> <tipx,y> <kneex,y> <textx,y> <text...> [height_mm]`, `sheettag <sheet> <x,y> <text> [bubble|square|diamond]`. Logged, undo-clean (`Inverse::PopSheet{Text,Leader,Tag}`), replay byte-stable.
  - **Scale-independence**: rendered in `crates/commands/src/pdf.rs` (the single shared preview + PDF pipeline). Sizes come straight through `mm()` (mm-on-paper × pt/mm) with NO viewport-scale term — unlike geometry, which passes through `world_to_paper_mm(_, view.scale)`. Test `sheettext_is_invariant_to_viewport_scale` asserts the emitted PDF font size is byte-identical at 1:50 and 1:500 and equals `5mm × pt/mm`.
  - **Left alone (as specified)**: model-space `Annotation` (Text/Hatch/LinearDim) for in-model notes; the `structure::Grid` column-grid datum.
  - Tests (7 new): scale-invariance (core), leader polyline + arrowhead + tag bubble geometry in PDF, round-trip serialize/load identical, undo pops each in reverse, export contains the annotations. `cargo test --workspace` green, clippy clean.

## Compliance plugins (engine SHIPPED 2026-09-03; packs still planned)

Shared foundation first, then two rule packs riding it. Advisory only — every
report carries "advisory pre-check, not a code review; verify with a licensed
professional / AHJ". Rule packs are data (JSON), never hardcoded, so editions
update without code and the deck LLM can read/extend them.

- [x] **M-checkengine** — SHIPPED 2026-09-03: declarative compliance-check engine (`crates/commands/src/checkengine.rs`).
  - Rule = pure JSON: id, code_ref, severity (error/warn/info), target query (kinds curve/mesh/block/door/stair/slab/wall/beam/column + layer + name_contains + story band), geometric predicate + threshold, message. Pack = named rule list, hard-validated on parse (malformed JSON, unknown kinds/severities, empty/duplicate ids, non-positive thresholds all rejected).
  - Engine emits `ComplianceReport` (per-rule verdict pass/fail/warn/info + object ids + locations + measured vs required + checked count), stored in `Document::compliance_reports` and served by the existing `report` verb (`report codecheck`; bare `report` includes it) — exact `AnalysisReport` plumbing, serde-back-compat for old files. The advisory disclaimer lives IN the report context so every rendering carries it.
  - Probes shipped (pure fns, each analytically tested): ramp/path slope (same math as the sitepath 1:12 advisory), sampled perpendicular clear width (BVH ray-casts at 1 m probe height), door clear width (param-block `width`×scale, else block-def plan extent — honest limits documented), riser extraction from stepped meshes (up-face z-level clustering), headroom (vertical ray-cast), turning-circle fit (simplified planar 60" disc in a person-height band), count-per-story (aabb-bottom banding). NOT shipped: travel-distance along route (deferred to the packs that need it).
  - Verbs: `codecheck <pack> [story]` — logged; the resolved rules AND marker ids are embedded into the op on first exec (CutFill precedent) so replay is disk-free and bit-stable; severity-colored failure markers (red/orange/blue circles) on the 'compliance' layer, undo-clean. `checkrules list|load <path>` manage packs (`~/.config/itsjustcad/checks/<name>.checks.json`; `codecheck` auto-resolves that dir on a miss). Registry-listed → deck-callable, GBNF-covered, menu/palette-visible.
  - Deck prompt section (CODECHECK_HELP) teaches codecheck → `report codecheck` → critique grounded in rule ids, lists every check-kind so the LLM can author packs (draft JSON → user saves → `checkrules load`), and hard-requires the advisory disclaimer ("never claim a design complies"). Prompt-completeness test pins it.
  - Embedded demo pack `assets/checks-demo.json` (6 rules: ramp ≤1:12 error, door ≥32in error, riser ≤7in error, headroom ≥80in warn, route width ≥36in warn, guard-drop >30in info) proves the engine end-to-end against a built violating scene. ~25 new tests (schema, probes, engine e2e, report serving, replay stability, undo, story filter, prompt completeness).
- [x] **M-ibc** — SHIPPED 2026-09-03: IBC 2021 rule pack (`assets/checks-ibc2021.json`, 11 rules, embedded + registered alongside `demo`) riding the M-checkengine engine, data-driven so a 2024 edition swaps in.
  - Room semantics: `room <closed-curve> <occupancy>` verb tags a closed-curve region with an IBC use group (assembly|business|residential|mercantile|educational|storage|institutional) + computed shoelace area, stored as `Document::rooms` (`Room` type: name, occupancy, area, boundary; serde-back-compat, undo/replay-stable via `Inverse::RoomList`); `rooms` lists them. Registry + parse + deck-callable + GBNF-covered.
  - New backward-compatible `CheckKind` variants (serde-tagged): `occupant_load` (room area ÷ IBC Table 1004.5 factor — factors are DATA in the pack JSON), `exit_count` (occupant load → required exits via IBC 1006.3.2 `[max_load, exits]` bands, also pack data; exits are name-matched "exit" objects within room reach), `travel_distance` (room centroid → nearest exit, HONEST straight-line + noted, IBC 1017.2 250 ft sprinklered), `tread_depth` (stair plan run ÷ riser count, IBC 1011.5.2).
  - Pack rules: riser ≤7in (1011.5.2), tread ≥11in (1011.5.2), stair headroom ≥80in (1011.3), stair width ≥44in (1011.2), corridor ≥44in (1020.2), door ≥32in clear (1010.1.1), guard-drop >30in (1015.2-3, name-match honesty note), habitable ceiling ≥90in (1208.2), occupant load (Table 1004.5), exit count (1006.3.2), travel distance ≤250ft (1017.2). Every message carries an IBC section ref + advisory disclaimer; guard/ceiling are name-matched only, travel distance is straight-line — noted, kept honest.
  - Prompt: `CODECHECK_HELP` extended with the ibc pack + room-tagging egress flow + the 4 new check kinds; completeness test green. ~10 new tests (room area/undo/replay, occupant-load 100m²@150gross→8, exit-count thresholds, tread-depth synthetic stair, travel-distance known layout, full pack e2e, pack validates).
  - Honest limits: handrail/guard/ceiling checks are name-match only (no geometric guard/handrail modeling); travel distance is straight-line, NOT the routed egress path; exit-count uses a proximity heuristic (exits within the room's half-diagonal), not door-graph egress. All disclosed in messages + module/prompt docs.
- [x] **M-ada** — SHIPPED 2026-09-03: ADA 2010 Standards / ICC A117.1 rule pack (`assets/checks-ada.json`, 12 rules, embedded + registered alongside `demo`/`ibc2021`) riding the M-checkengine engine, data-driven.
  - New backward-compatible `CheckKind` variants (serde-tagged): `ramp_landing` (flat-segment landing detection along a ramp centerline — flags a run rising >`max_rise`/30 in without a ≥`landing_len`/60 in near-flat landing, `missing_landings` probe, ADA 405.7), `threshold_height` (door step ≤`max` from the door block `threshold` param, else honestly flagged "not modelably detectable", ADA 404.2.5), `max_cross_slope` (perpendicular grade of a ramp MESH surface via up-facing face normals, run/cross axes inferred from AABB, ADA 403.3/405.3), `count_ratio` (accessible count vs total via ADA Table 208.2 `[max_total, min_accessible]` bands — DATA, name-match heuristic). Each has an analytic unit test (ramp with/without landing, threshold param/none, cross-slope flat vs tilted plate, ratio bands).
  - Pack rules: ramp running slope ≤1:12 (405.2), ramp cross slope ≤1:48 (405.3), ramp landings (405.7), route clear width ≥36 in + 32 in pinch (403.5.1), door clear width ≥32 in (404.2.3), threshold ≤½ in (404.2.5), 60 in turning space (304.3.1), clear floor space 30 in (305.3, only width probed), handrail height ≥34 in (505.4, name-matched), accessible parking count Table 208.2 (406/curb slope 406.1). Every message carries an ADA section ref + advisory disclaimer.
  - Prompt: `CODECHECK_HELP` extended with the ada pack + accessible-route flow (name ramp-1/route-1/turn-1/parking-N) + the 4 new check kinds; completeness test green. Tests: 5 new probe/pack tests in checkengine + ADA pack e2e on a violating scene (ramp passes running slope but fails landings, narrow door fails clear width, threshold flagged) with replay stability.
  - SKIPPED (honest, documented in module + pack + prompt): reach ranges and door maneuvering clearances — the model carries NO fixture-height or door-swing geometry, so any number would be fabricated; restroom fixture clearances and stair handrail *extensions* likewise omitted (no fixture/handrail-end geometry). Threshold is param-only (no small-step mesh geometry); cross slope needs an actual ramp mesh (a drawn centerline carries none); handrail height is name-matched. All limits disclosed.
  - Reuses M-landscape ramp/slope math (`steep_segments`) and check-engine probes; site + building both.

## Interop (M7 remainder) — COMPLETE ✅ (audited + closed 2026-09-02)

Audit found nearly all of M7 had already shipped; the two real gaps (LAZ,
.3dm export) closed this session.

- [x] **IFC** — import (IFC4/IFC2x3, meshes → 'ifc' layer, semantic variant for M-bim-import) + export (IFC4). *(already shipped)*
- [x] **3DM** — pure-Rust openNURBS reader (meshes/lines/polylines/NURBS curves with names + layers; breps skipped) — no opt-in download needed. Export shipped 25d004a: the spec-conformant V5 writer promoted from test-only to `export .3dm`.
- [x] **SVG / CSV export** — shipped earlier (svg.rs, csv.rs, wired to dialog + registry).
- [x] **Point clouds** — LAS 1.2–1.4 (hand-rolled) + E57 (e57 crate) already in; **LAZ decompression added b5223c4** via laz-rs (Apache-2.0), batch-decompressed so decimation to ≤200k never materializes the cloud.
- [x] **Mesh import** — OBJ/STL/glTF/GLB/Collada .dae all shipped (mesh_import.rs, hand-rolled parsers with size caps).
- [x] STEP AP242 import + faceted export — implemented via OCCT, but **gated behind the opt-in 'kernel-occt' feature** (M-kernel); default builds report a clear "needs the exact-BREP tier" error.
- DWG / SKP / RVT: bridge via open exchange only (no proprietary readers — license clean).

## Assisted-import bridge (SHIPPED 2026-09-05)

- [x] **M-dwg-bridge** — DONE. `import *.dwg` via an auto-invoked, license-clean converter, wider DXF block coverage, a scoped folder the deck can operate in, and a **warning-UX** on import. All pieces landed with tests; `cargo test --workspace` green, clippy clean.
  - **DONE — Import warning state** (`crates/commands/src/{dwg,exec}.rs`, `crates/app/src/{app,headless}.rs`): an import that finishes but drops entities (unsupported/malformed) or whose DWG conversion produced LibreDWG soft warnings now completes in a **warning state**, not an unconditional success tick. `dwg::count_libredwg_warnings`/`conversion_warnings` count soft `ERROR`/`warning` stderr lines even when LibreDWG exits 0 with a complete DXF (the 000-BG.dwg ADT case: 2694 warnings, valid output). `exec::ImportSummary { path, entities_imported, entities_skipped, warnings }` + `has_problems()` (true iff skipped>0 or warnings nonempty) is stashed on the `Session` (`last_import`, taken via `take_last_import`) by every entity import (DXF + DWG). The GUI popup (`app::ImportOutcome` / pure `ImportPopupState::{Success,Warning}` decision) renders green "✔ Imported" for a clean import and an amber ⚠ "Imported with warnings" heading + imported/skipped counts + converter-warning lines otherwise; the batched-DXF completion path builds the same outcome. Headless echoes the summary to stderr so the warning state is scriptable. Broadened per plan: **any** lossy import (DXF proxy/unsupported entities too), not just DWG, flags the warning.
  - **DONE — Bounded DWG import** (`crates/commands/src/dwg.rs`): `resolve_dwg2dxf()` probes the well-known dirs (/usr/local/bin, /opt/homebrew/bin, /usr/bin, ~/.local/bin) then PATH, mirroring the LLM-CLI resolver. `convert_dwg_to_dxf` shells out with FIXED args (`dwg2dxf -o <tmp>.dxf <input>` — no shell, no interpolation, no model-controlled flags), into a per-invocation temp dir cleaned up on every path. Absent binary → "install LibreDWG to import DWG (brew install libredwg)". Wired into `exec.rs::import` on the `.dwg` extension.
  - **DONE — Truncation detection** (`validate_dxf_complete`): after conversion we do NOT trust the exit code — the DXF must have BOTH an `ENTITIES` section AND an `EOF` marker, else "DWG conversion incomplete (converter too old or unsupported DWG — try a newer LibreDWG/ODA)". This catches the exact LibreDWG-0.13.3 silent-drop failure. Never a silent empty import, never a panic.
  - **DONE — Wider DXF block coverage** (`dxf.rs` `parse_blocks`): HATCH bodies → boundary polyline; SPLINE → tessellated polyline (fit points, else control points); nested INSERT → the referenced block's geometry BAKED in at the insert transform (BlockGeometry is deliberately flat, so baking not referencing) with a cycle guard; a block mixing mappable + unmappable bodies keeps what maps. SPLINE also added to the top-level ENTITIES path.
  - **DONE — Scoped deck workdir** (`crates/commands/src/workdir.rs` + `Command::Workdir`/`WorkdirFiles`): `workdir <path>` grants ONE folder (persisted to `~/.config/itsjustcad/workdir.txt`), `workdir`/`files` list it, and `import <bare-name>` resolves inside it — path-traversal guarded (absolute paths, `..`, separators, and symlink escapes all refused). Registry-registered so the deck can drive it. Bounded verbs only — never a shell, never whole-filesystem.
  - **DONE — Headless query echo**: read-only query verbs (non-logged commands: blocks, files, workdir, bbox, area, import summary, …) now echo their outcome message to stdout in headless mode, so DWG/DXF round-trips and workdir listings are scriptable/testable.
  - **RESIDUAL — LibreDWG version reality**: the assisted path is only as good as the installed converter. LibreDWG 0.13.3 remains too old for AutoCAD-2013 ADT DWGs (drops ENTITIES); our truncation check now surfaces that as a clear error instead of a silent empty import, but a genuinely-newer LibreDWG or an ODA-based converter is still required to read such files. The optional real-binary end-to-end test is `#[ignore]` (needs a `.dwg` fixture + installed `dwg2dxf`); set `ITSJUSTCAD_DWG_FIXTURE` to run it. Nested-INSERT scale is uniform (DXF 41); non-uniform 42/43 collapse to it, matching the rest of the importer.

### Original spec (for reference)

- **M-dwg-bridge** — `import *.dwg` via an auto-invoked, license-clean converter, plus a scoped folder the deck can operate in. Turns the current two-step (user converts DWG→DXF by hand, then imports) into one verb the LLM can also drive — WITHOUT giving the model a shell.
  - **Bounded DWG import**: `import site.dwg` detects a `dwg2dxf` binary (LibreDWG) the way the app resolves `claude`/`ollama` (PATH + common locations); if absent, a clear "install LibreDWG to import DWG (`brew install libredwg`)" message, never a silent fail. Converts the *specific referenced file only* into a temp dir with fixed args (no model-controlled shell string), then feeds the existing hardened DXF importer → DWG blocks land as real block definitions + instances. **LibreDWG is GPLv3 → detect-and-shell-out to a user-installed binary; do NOT bundle or link it** (same detect-don't-ship stance as the LLM CLIs; keeps the AGPLv3 app clean).
  - **Scoped deck folder access** (optional, separate): a user-granted `workdir` the deck can list/import within — NOT whole-filesystem, NOT arbitrary shell. Lets the LLM find + import files in one place.
  - **Security invariant** (ties to [[M-secreview]]): the LLM never gets a general exec/filesystem plane. Every external-tool call is a narrow purpose-built verb with fixed arguments on a user-referenced file — auditable, injection-safe. Imported names still sanitized before reaching the prompt. This is the model for ANY future external-tool integration (SKP via a converter, etc.): a bounded verb, never a shell.
  - Tests: converter-detection (present/absent), fixed-arg invocation (no shell interpolation), temp-dir cleanup, DWG-origin DXF round-trip (blocks survive), workdir path-traversal guard.
  - **Real-world findings (2026-09-04, tested a 38 MB AutoCAD-2013 ADT `000-BG.dwg`):** (1) **LibreDWG 0.13.3 is too old** — it cannot convert this Architectural-Desktop DWG, silently drops the whole ENTITIES section (exit 0 but truncated, 4291 hard errors, unstable AEC proxy classes). M-dwg-bridge must **require a newer LibreDWG** (or an ODA-based converter) and **detect the truncation** (missing ENTITIES / EOF) rather than trusting exit code — surface "converter too old / file unsupported," never a silent empty import. (2) **DXF importer block-content gap** (`dxf.rs` `parse_blocks`): it only commits blocks whose bodies are flat LINE/POLYLINE/TEXT and **drops HATCH / SPLINE / nested-INSERT / mesh block bodies** — real architectural blocks lose most geometry even from a *valid* DXF. Widen `command_to_block_geometry` coverage (hatch boundary, spline→polyline, nested insert) before claiming real-DWG block import. (3) **Ergonomic:** `blocks` query verb prints nothing in headless — make query verbs echo to stdout under headless so round-trips are scriptable/testable. NOTE: the DXF *export* block round-trip (09abdd0) itself is verified sound — positive control (define→insert→export→re-import) passes with exact transforms; these three are converter/importer-coverage gaps, not an export regression.

## Long-term / research (audited 2026-09-02 — most had ALREADY shipped)

- [x] **M-kernel** — DONE as designed: opt-in OCCT tier exists (`kernel-occt` crate behind the `kernel-occt` feature; STEP + exact booleans route through it, default builds report the clear "needs the exact-BREP tier" error). Mesh kernel stays default. No further work planned.
- [x] **M-plugins** — DONE (was already shipped; verified): declarative JSON macros (params + command-template body, NO native code), persisted to `~/.config/itsjustcad/plugins/`, loaded at startup, `plugin list|reload|save|define|delete` verbs, menu integration, path-traversal + prompt-injection hardening, and the deck system prompt teaches the LLM to author macros mid-chat via `plugin define {json}` (17 tests).
- [x] **M-nurbs** — COMPLETE this session: interpolation (`interpcurve`) + control-point editing (`setpoint` + draggable UI) were already in; **added knot insertion** (`insertknot`, Boehm on homogeneous coords, shape-exact for rational curves) and **curvature graph** (`curvature` — comb hairs + tip curve on 'analysis', Menger curvature, reports max κ / min radius). Not done (niche): degree elevation; NURBS-output rebuild (rebuild emits polylines).
- [x] **M-solids2** — COMPLETE this session: sweep2, rail revolve and variable-radius pipe were already in; **added loft with guide curves** (`loft <profiles> guides <sel>` — skin bows through open guides, cosine falloff) and **blend surface** (`blend <a> <b> [bulge]` — Hermite-eased sheet, bulge 1 = ruled).
- [x] **M-constraints** — CORE SHIPPED 2026-09-02 (audit: was truly not started). New pure-std `crates/constraints`: 2D parametric sketch solver, Newton–Raphson with Levenberg-Marquardt damping (SolveSpace approach), numeric Jacobian, rank diagnostics (DOF count, redundant-constraint blame, per-constraint conflict list); 19 constraint kinds; 32 tests incl. 25 scrambled-start convergence runs. Command layer: `constrain <kind> <sel> [sel] [value]` (14 kinds — coincident/horizontal/vertical/distance/length/angle/parallel/perpendicular/equal/radius/fixed/tangent/midpoint/on; polymorphic by target type, nearest-endpoint resolution latched into the stored constraint, solves immediately), `solveconstraints`, `constraints list|delete <n>|clear`. Undo/redo-able, op-log replay-stable, deck-callable, solver status (solved / N DOF / redundant / conflicts) in the command output. Works on lines + circles/arcs. Remaining (niche until a sketch-mode UI exists): constraints on polyline vertices / NURBS control points, `symmetric` via command line (solver supports it), constraint glyphs in the viewport, drag-with-constraints.
- [x] **M-site / M-basemap** — mostly DONE (was already shipped; verified): `terrain <csv|geojson>` from contours, GeoJSON/OSM import, OSM + keyless-satellite slippy-tile basemaps (offline-first, transient, never op-logged), `location` georeferencing.
- [x] **M-assets** — COMPLETE this session: block library (`blocklib/blockload/blocksave` + 6 seeded starters), 7 drafting hatches and Hershey vector drafting font were already in; **added the ANSI standard hatch set** (`hatch <sel> ansi31..ansi38 [spacing]` — iron/steel/bronze/plastic/fire-brick/marble/lead/aluminum; renders in viewport + PDF, scales with the object, replay-stable). Not done (niche): TrueType drafting fonts, user-defined hatch rules.
- [x] **M-dynblocks** — DONE (was already shipped; verified): `pblock <name> [param=default ...] : templates` parametric blocks, `insert ... param=value` overrides, `param <sel> key=value` re-derives geometry. No constraint-driven grips (see M-constraints).
- [x] **M-cloudfiles** — COMPLETE this session: atomic saves (write-temp + fsync + rename) and crash journal were already in; **added external-change detection** (mtime/size watch, throttled 2 s poll, Reload-vs-keep-mine modal, deleted-file notice) and **conflict-copy awareness** (Dropbox "conflicted copy" + numbered-duplicate siblings warned at open/save).
- [x] **M-histedit / M-options** — DONE (was already shipped; verified): `amend <step> <command>` op-log rewriting with full replay, `option save/list/switch/delete` design-option branches persisted in the file format. Not done: arbitrary rebase-style history surgery (amend-only by design).
- [x] **M-i18n / M-docs** — **i18n string layer + Spanish SHIPPED**; **docs half (tutorials/samples/landing/screenshots) SHIPPED** (2026-09-07). Spanish-language doc translation DEFERRED (docs remain English-only for now).
  - **Done (M-i18n):** hand-rolled keyed catalog `crates/app/src/i18n.rs` (pure Rust, no deps): `Lang{En,Es}`, thread-safe atomic current-locale, `t("namespaced.key")` with English-then-key fallback, locale-aware `fmt_num`/`decimal_sep`. ~90 keys extracted (menu bar titles + File/Edit/View/Theme/LLM/Window/Help items, dialog/About/onboarding/panel-tab/status/button strings) with full **Spanish** translations (architect/CAD terms: capa, cota, sólido, malla, croquis…). Full menu bar routed through `t()` (biggest surface). Language chosen via `language en|es` verb (alias `lang`), a Settings (Model Setup) combo box, persisted to `ui.json` (`"language"`), applied at startup before first render; live switch. Completeness test asserts every `en` key has `es` + fallback behaviour. `cargo test/clippy --workspace` green.
  - **Deferred (follow-up i18n sweep):** deeper command-line feedback/error strings, rfd import/export filter labels (format proper-nouns), status-bar coordinate readout via `fmt_num` (helper ready, not yet wired through doc-crate `format_length`), deck/onboarding modal bodies. Menu bar + dialogs prioritized as high-visibility.
  - **Done (M-docs, 2026-09-07):** four end-user tutorials `docs/tutorials/` (getting-started → PDF sheet; site planning with intemfit; environmental analysis; working with the deck), cross-linked to the command-reference and verified against `crates/commands/src/registry.rs`. Three ready-to-open sample documents `examples/*.itsjustcad.json` (massing, courtyard-sheet, full intemfit site) + generating `.script`s + `examples/README.md`, all produced/verified via the headless binary (`--run … --out …`). Value-prop screenshots generated headless into `docs/screenshots/` (massing, pencil plan-cut section, sun-hours heatmap, generated site plan) and referenced in README + tutorials. Self-contained landing page `docs/index.html` (embedded CSS, no CDN, GitHub-Pages-ready). README refreshed for v0.4.0 with a Documentation section (tutorials + landing + reference), the new screenshots inline, and the advisory-only framing kept.
  - **Deferred (M-docs):** Spanish translation of the docs/tutorials/landing page (docs are English-only); two GUI-only value-prop screenshots that the headless renderer cannot capture — the live deck chat panel and skinned UI chrome (existing `docs/shot-deck.png` / `docs/shot-ui.png` stand in); a dimensioned-**sheet** screenshot (paper-space dims/text don't render in the offscreen renderer — the courtyard sample ships a printable A3 `plan-01` sheet and the PDF path is verified instead).

## Pre-release

- [x] **M-betareview / public beta-review** — SHIPPED (2026-09-08): public, self-contained `docs/beta-review.html` — a comprehensive tickable manual-test checklist for beta users covering every v0.4.0 area (modeling, expressive structures, intemfit, environmental analysis, compliance pre-check, landscape/planting, DWG import, deck, i18n/skins, sheets/PDF, interop, render, UI/menus/gumball). Sticky TOC, collapsible sections, localStorage-persisted checkboxes + per-section notes, and an **Export results** button that serializes state to a labeled JSON download (or clipboard copy) for the tester to email back. Named to dodge the `review*.html` gitignore (basename `beta-review`). Linked from the README **User Beta Review** subsection and the `docs/index.html` landing page. Derived from the internal `review2.html` rev d; verbs verified against `crates/commands/src/registry.rs`.
- [x] **PRE-PUSH PASS** — DONE (2026-09-07): version bumped 0.3.0→0.4.0 (workspace + bundle script CFBundle*); repo URL owner fixed to `htarrido-picart/itsjustcad` (+ homepage); README updated (i18n EN/ES, DWG assisted import, site-planning `lot*` verbs) with the massing value-prop screenshot `docs/shot-massing.png` referenced (5 prior screenshots already in README: ui, plan-pencil, sun-shadow, elevation, deck, viewport); SECURITY.md audited (M-secreview; DWG-converter trust-boundary note added). Gate: `cargo test --workspace` fully green (2053 passed, 0 failed); `cargo clippy --workspace` 0 warnings. Release build + dist bundle refreshed, ad-hoc codesigned + verified. Pushed **main** + annotated tag **v0.4.0** to origin.
- [x] **M-secreview / M-codereview** — untrusted-input hardening pass (2026-09-03; rerun-before-each-release). AUDITED every parser + JSON loader for OOM/overflow/index-panic/traversal/prompt-injection.
  - **Fixed** (regression test per fix, all in mesh_import.rs): binary-STL `84 + 50*tri_count` unchecked multiply (wrapped past the length gate → parallel over-read) → checked_mul/add; GLB `data_start + chunk_len` wrap past the bounds check → checked_add; Collada `<triangles>`/`<polylist>` file-supplied input-offset > stride + vcount·stride usize overflow (panic on index) → `.get()` guards + checked arithmetic. Commit 5129bd8.
  - **Verified already-safe** (audit surfaced these; confirmed hardened, no change needed): point clouds (LAS/LAZ/E57) — `MAX_POINTS` decimation, `record_length`/offset validated vs file len, LAZ tolerates truncated tail; 3DM (rhino3dm) — `cap_hint` clamps allocs to remaining buffer, NURBS `order`/`cv_count` range-validated *before* the knot arithmetic, no trusted deflate path; GeoJSON/Overpass — `serde_json` depth-limited parse returns clean Err; JSON loaders (plugin/block/checkpack) — every name→path flow validated (rejects `/` `\` `.`/`..`) *before* `.join`, with existing traversal tests; **prompt injection** — SECURITY.md's sanitization claim VERIFIED true: single choke point `digest::sanitize_name` strips backticks/control/newlines, collapses whitespace, caps to 64 chars, wraps in «» guillemets; imported names cannot forge a `draft` fence.
  - **Accepted low** (documented in SECURITY.md, no fix): SD `ControlImages::from_prefix` reads `<prefix>_depth.png` from an app-generated prefix in a private dir (fixed suffix + internal prefix → not attacker-reachable); config files read whole into memory (user's own dir, small by design).
  - **Docs:** SECURITY.md rewritten — supported versions, report to htarrido@pm.me / GH advisory, expanded threat model, "what is hardened", re-run checklist. No stale security-review doc existed to `git rm` (only PRE-PUSH PASS references one).
  - **Remaining / future rerun:** DXF and EPW parsers use safe split/parse patterns (no unbounded alloc or index panic found) but have no dedicated adversarial corpus yet; a broader fuzz harness (arbitrary/cargo-fuzz over each importer) is the natural next step.
- [ ] **M-sign** *(deprioritized)* — Apple + Windows code-signing certs to kill first-launch warnings. Only when marketing to non-technical architects at scale.

## Out of scope (this repo)

- iOS/iPadOS/mobile app — separate private/paid repo consuming the shared egui-free Rust core.
- Web/WASM viewer — dropped.
