# ItsJustCAD — Phases Remaining

Status as of 2026-09-01. Shipped milestones (M2–M5 core, layers, gumball,
undo, dims/sheets/PDF, DXF/glTF/OBJ/STL, local-LLM onboarding, per-doc chat
persistence, native menus, clipboard) are omitted — this tracks what's LEFT.

## Design system (report batches A–F) — COMPLETE ✅

HIG-grounded design revision, run as sequential batches. All six landed.

- [x] **A — Tokens + a11y foundation** — `surface_elevated`/`on_surface_tertiary`/`destructive` roles, WCAG contrast helper + passing ≥4.5:1 body-role tests, type scale (caption/title/weight), spacing guards. *(already implemented; verified)*
- [x] **B — Iconography** — Lucide clean, de-dup guard, `Line→pen-line`; added `Paperclip` icon replacing 📎. *(shipped ce0d1e1)*
- [x] **C — Menus + keyboard** — Window menu, shortcuts shown, disable-don't-hide, stateful View menu (Panel Hide/Show ⌘\, display/lighting radios). *(shipped f470de7)*
- [x] **D — Component correctness** — button roles (destructive/pressed/"…"), segmented controls + toolbar grouping, unsaved-changes + neutral-title alerts. *(already implemented; verified)*
- [x] **E — Ergonomics + states** — empty-doc viewport hint + add-layer toolbar to top (rest already present: empty chat, hit-target floor, focus ring, middle-truncate, striped rows). *(shipped 36e6418)*
- [x] **F — Polish** — killed dead `HISTORY_H` (history height already token-relative), wired `reduce_motion` to spinners, corrected download-cancel warning (partial `.part` is kept for resume; autosuggest accent fill already present). *(shipped)*

## Queued — small, high-impact

- [x] **Pending deck usability** — download-that-can't-run (disk-space gate + unknown-RAM warning; RAM gate was already in), download wired to active deck even with Model Setup closed (per-frame terminal poll), model-mismatch surfaced for Anthropic too + suffix-tolerant matching (`qwen3` ↔ `qwen3:latest`), active model always visible in the chat header, download failures announced in the deck transcript. *(shipped; builds on 5d16875)*
- [ ] **Local model finetune** — 0.6B too weak; default 4B Qwen; free synthetic training data from the command registry.
- [x] **Chat encryption** *(deferred, now shipped)* — chat-session files (`~/.config/itsjustcad/chats/<uuid>.json`) encrypted at rest with AES-256-GCM, keyed by a per-user 256-bit data key in the OS keychain (`keyring` crate). Transparent auto-unlock (no prompt). Self-describing `ICEC` magic-header container (magic ‖ version ‖ 96-bit nonce ‖ ct); legacy plaintext stores load and upgrade to encrypted on next save. Plaintext fallback with a one-time warning when no keychain (headless/CI/bare Linux) — never a data loss or hard fail. Key store is an injectable `KeyStore` trait so tests never touch the real keychain. See `crates/app/src/chat_crypto.rs`. **User-controllable, opt-in**: `chatencryption on|off` (alias `encryptchats`) toggles at-rest encryption, persisted to ui.json, **default OFF** — the app isn't code-signed, so an always-on default would prompt every user for keychain access on first chat (revisit once M-sign lands). Turning it on/off is lossless either way (self-describing format loads both). The per-process key cache means at most one keychain prompt per launch when enabled.

## Core UX debt

- [x] **M-layerspanel** — Rhino-style 7-column layers table + lock/linetype. *(verified shipped)*
- [x] **M-uilayout** — Rhino docking layout. *(mostly landed; verified)*
- [ ] **M-legacyskin / M-uipolish** — onboarding-driven skins (AutoCAD/Rhino/Revit theme + fonts + aliases), app icon (all platform sizes), toolbar iconography, ergonomics pass. Largely == design batches A–F.

## Big domain adds

- [x] **Camera projections** — COMPLETE (audited 2026-09-02; core had already shipped). Perspective FOV via `camera <mm>` full-frame lens math, 2-point (`camera 2point`, projection shear — verticals stay vertical), equirect panorama + equidistant fisheye (cubemap capture + remap), phone-lens sims (iPhone/Pixel/Galaxy ultrawide/main/tele as 35mm equivalents). Gap closed this session: stateful **View ▸ Camera radios** (Perspective / Two-Point / Panorama 360° / Fisheye; checked from the live camera, fire the same `camera` verbs the deck uses).
- [x] **Render & colors** — COMPLETE (audited 2026-09-02; had already shipped). Pencil display mode + sketchy NPR edges, `sketchup` preset verb (sun shading + profile edges + sky/ground gradient), lighting rework (Working = hemispheric-diffuse default, Sun = SPA key light, Presentation = specular opt-in), per-object `color` + ColorMode (by-layer/by-object/by-type/random).
- [x] **M-enviro** — COMPLETE (closed 2026-09-02): `shadowstudy`, `sunhours` grid heatmap, `facesunhours` per-face insolation, EPW import (sets location + reports stats), `sunpath` yearly dome, `radiation` annual kWh/m²·yr per face (EPW DNI/DHI month×hour bins, occlusion-tested, bins embedded in the op-log). **Deck critique hooks closed this session**: every analysis now stores a compact structured `AnalysisReport` on the document (min/avg/max, 6-bin distribution, worst/best sample locations with compass facings — token-frugal, regenerated on replay); new read-only `report [analysis]` registry command serves it to the deck; system prompt gained an "Environmental critique" section teaching analysis → report → design feedback (sun-hour thresholds, radiation hot/cold faces, shadow coverage) so the LLM grounds critique in sampled locations ("north face at (0,10,2) gets 0.5 h — don't put the terrace there").
- [x] **Diffusion render** — COMPLETE (2026-09-02). Control-image export (`controlimages <prefix>` → depth/edge/mask PNGs from the current view) + cassette-style SD backends speaking their real protocols (ComfyUI graph/upload/history-poll, A1111/Forge/Draw Things `img2img`+ControlNet, Replicate cloud predictions) + the end-to-end `render <prompt…>` GUI flow: capture control images from the active view → async job on the tokio runtime → result PNG saved to the private runtime dir and shown in a floating "AI Render" window. `render cancel` really aborts (task drop); setup surface is `render backends` / `render use <name>` / `render test` (reachability probe with "cannot reach X — is it running?" guidance). All errors land on the command line, never silent. Per-protocol mock-HTTP-server tests; headless warns-and-ignores. Ships with NO backend active — backends are user-configured in `render_decks.json`.
- [x] **M-structural** — COMPLETE (audited + closed 2026-09-02). Audit: nearly all had ALREADY shipped — verbs (grid/story/beam/column/slab/wall/load/support), **IFC-structural export** (typed IfcBeam/IfcColumn/IfcSlab/IfcWall with swept-solid bodies + full IfcStructuralAnalysisModel: IfcStructuralCurveMember/SurfaceMember, IfcStructuralPointConnection with boundary conditions, IfcStructuralLoadGroup with point/linear/planar actions), **SAF export**, and **M-bim-import** (IFC import reconstructs typed Frame/Area members with stories/materials via `import_semantic`; mesh fallback for the rest). Closed this session: SAF upgraded from a nonstandard ZIP-of-CSV to a **genuine SAF 2.2.0 .xlsx workbook** (hand-rolled OOXML on the existing store-mode ZIP writer — no new deps; 9 SAF sheets, numeric cells native, `.saf`/`.xlsx` both accepted, export dialog fixed from a dead "xml" filter), plus the **never-analyze disclaimer** embedded in both formats (xlsx docProps description + IFC FILE_NAME header note: "geometry+topology handoff — no analysis results"). Interop only — the app never claims to analyze.
- [x] **M-expressive** — COMPLETE (audited + closed 2026-09-02). Audit: the shared engine ALREADY existed (`kernel-mesh/src/formfind.rs`) and funicular/tensegrity/cablenet already routed through the one `dynamic_relaxation`; nothing needed re-unifying. Closed this session: engine hardening (per-node point loads, true Barnes/Day kinetic damping option, Schek force-density solver — deterministic Gauss–Seidel) + hard tests (settled sag vs analytic catenary ≤2%, kinetic == viscous equilibria, pinned-bar stability, perturbed-start uniqueness, net symmetry), and **Frei Otto minimal surfaces shipped** — `minsurf <closed-curve-sel> [n]` (aliases soapfilm/minimalsurface) stretches a soap film (zero-rest-length force-density harmonic net) across any closed boundary curve; undo/replay-stable, deck-callable. Deliberately left analytic (closed-form is exact; relaxation adds nothing): hypar, geodesic, spaceframe, gaussvault (exact catenary section), gridshell (rides analytic surfaces).

## New phases (added 2026-09-02)

- [x] **M-landscape** — core landscape design tools SHIPPED 2026-09-03 (aa47c7b…fe315a6; pure math in `commands/src/landscape.rs`, all verbs registry/GBNF/deck-callable, undo + replay-stable, analytic-terrain tested):
  - *Contours OUT* DONE: `contours <interval> [major-every]` — marching-triangles extraction from the terrain mesh, chained polylines on 'contours'/'contours-major'. NOT done: contour labels, sheet/PDF feed.
  - *Grading & earthwork* DONE: `pad <x,y> <w> <d> <elev> [slope]` (flat pad + side slopes to daylight, first pad snapshots pre-grading heights) and `cutfill` (cut/fill m³ TIN prism estimate, heights embedded in the logged op, AnalysisReport → `report cutfill`). NOT done: freeform `grade` regions, retaining-edge detection.
  - *Planting* DONE: embedded 12-species catalog (`assets/plants.json`, real data), `plant <species> <pt> [age-years]` (age-scaled trunk+canopy mesh draped on terrain, layer 'planting', occludes sun analyses automatically), `plantrow <a> <b> <spacing>`, `plantschedule <path.csv>` + AnalysisReport. NOT done: `plantmass <region>`, seasonal deciduous transparency.
  - *Water & drainage* DONE (advisory-only wording throughout): `flowarrows [n]` steepest-descent glyphs + `ponding` local-minima markers, both with AnalysisReports. NOT done: watershed accumulation, `swale`.
  - *Hardscape* DONE: `sitepath <curve-sel> <width>` ribbon draped on terrain with 1:12 accessible-slope warning. NOT done: stairs/ramp generators, site sections through terrain.

- [x] **M-deckagent** — deck LLM interactivity + agent harness (closed 2026-09-02):
  - *Terse mode* DONE: `TERSE_STYLE_HELP` + `terse_adjusted()` (hard `TERSE_MAX_TOKENS` cap plumbed into the openai_compat/anthropic wire bodies); per-cassette `DeckConfig.terse` override, default ON for local OpenAI-compat, OFF for cloud; LLM ▸ Terse Replies checkable toggle (in-window + native), persisted in decks.json.
  - *Clarify-before-act* DONE: `QUESTION: <text>` message form (`agent::parse_question`), CLARIFY_HELP in the full prompt + condensed rule in the brief local prompt, accent-bordered Question entry in the transcript; user's next message continues the turn. Mixed commands+QUESTION fails safe to plain chat.
  - *Plan-execute harness* DONE: `agent::{Plan, PlanStep, parse_plan}` + `tool_loop::run_plan_loop` (Plan/Question step decisions, MAX_STEP_ATTEMPTS per-step retry, mid-run replan, cancel-preserves-state, round budget); streaming path mirrored in deck_pane (checklist Entry::Plan ticks live, step-per-turn auto-continuation, verify turn with bbox/schedule, plan persisted in the per-doc draft → resumes after relaunch, Stop pauses). GBNF root now `draft | question | plan`.
  - NOT done (deliberate cuts): `run_plan_loop` is not yet the live execution path (the streaming deck_pane driver is; the loop is the future FOSS-harness core and is fully mock-tested); no dedicated per-step GBNF "STEP" form (steps are plain draft blocks — the existing fence grammar covers them).

- [x] **M-chatmd** — rich markdown in the deck chat transcript (closed 2026-09-02):
  - *Rendering* via `egui_commonmark` 0.24 (MIT/Apache, matches workspace egui 0.35): deck bubbles + the streaming bubble render **tables as striped grids** (the headline — pairs with the `report` verb for analysis stats/schedules), plus bold/italic, inline + fenced code, lists, headings, rules. Chosen over a hand-rolled parser because it slots into the existing bubble frames without touching the custom Question/Plan/Status entry styles (base integration 618f561).
  - *Prompt nudge*: `TABLE_HELP` "## Presenting data" section in the full system prompt (pipe tables for object lists/stats/schedules) + a "tables ARE terse" rule in `TERSE_STYLE_HELP`; brief local prompt untouched (GBNF-constrained draft/question/plan forms — tables come from cloud models and `report`).
  - *Copy*: right-click "Copy message" on User/Deck/Question/Plan bubbles copies the RAW markdown source (`entry_copy_text`), so pasted tables stay tables.
  - Tests: prompt-nudge + terse-interaction tests, `entry_copy_text` unit test, `chat_markdown_table` kittest preview snapshots (dark + light).

- [x] **M-dyntabs** — dynamic Blocks + Plugins right-dock tabs (closed 2026-09-03). AUDIT CORRECTION: the premised "dynamic sheets-tab pattern" did not exist — sheets are doc-side only (`sheet`/`sheetview`/`print`, no UI tab anywhere in crates/app); the real precedent generalized was the FIXED 3-tab right-dock strip (`tabstrip::PanelTab`).
  - *Tab registry*: `PanelTab::FIXED` + dynamic `Blocks`/`Plugins` variants; `TabState::visible_tabs(has_blocks, has_plugins)` (content OR user-pin OR active keeps a dynamic tab; empty+inactive un-pins so it disappears; reappears with content), `sync_dynamic` reconciled per frame; `strip_ui` draws a caller-supplied list so dynamic tabs share the exact folder-tab look/collapse/width contract. Note: Blocks is de-facto always visible — `Session::default` seeds the 14 starter pblocks, which ARE definitions.
  - *Blocks tab*: rows from pure `dyntabs::block_rows` (name, live instance count via plain-name/`source` match, geometry count, pblock `name=default` signature; `__param/` bakes filtered). Insert prefills `insert <name> ` (placement by typing), library section lists `blocklib` (cached, ↻ re-scan) with per-row `blockload`, Delete fires the NEW guarded `blockdelete` verb — op-logged substrate command refusing while instances exist (button pre-disabled with count tooltip; confirm step otherwise), undo restores plain+parametric entries, replay-stable.
  - *Plugins tab*: rows from `dyntabs::plugin_rows` (usage, summary, pretty JSON). Run prefills the verb, JSON opens a read-only monospace popup, Reload/Delete ride the existing `plugin reload|delete` verbs, Open-folder reveals `~/.config/itsjustcad/plugins/`.
  - *Deck integration*: UI-plane grammar grew `panel chat|sessions|layers|blocks|plugins` (`UiAction::PanelTab`, transient — applied then stripped from ui.json so a relaunch never re-forces a tab); `UI_VERB_HELP` advertises it; both plane-completeness tests extended. `ITSJUSTCAD_PANEL_TAB` accepts the new names.
  - Both tabs are VIEW + verb-trigger surfaces only — every mutation goes through the one substrate/command-line path; op-log/undo/replay untouched.
  - NOT done (deliberate cuts): no per-tab close button or reorder (the fixed tabs have neither — dynamic tabs disappear by emptiness instead); no kittest snapshot of the new tabs (registry logic + derivations are pure-tested; snapshot infra would need seeded doc state for marginal value).

## Performance (planned 2026-09-02)

- [~] **M-perf** — STEP 1 SHIPPED 2026-09-03 (d6290ab, 6bd87e3); step 2 (GPU) deferred by design. Parallelize hot loops with rayon (MIT/Apache, deterministic, replay-safe), GPU compute only if profiling still hurts after:
  - [x] *Step 1 — rayon on analysis loops* (closed 2026-09-03): `sunhours` grid ray casts, `facesunhours` + `radiation` per-face scoring, `shadowstudy` time frames, raw-LAS point decode, binary-STL triangle decode — all via shared per-item kernels with ordered `collect` (parallel output bit-identical to the `_seq` references kept for tests; no parallel float reductions). Determinism tests per site (par == seq + repeat-run) plus an `#[ignore]` bench (`bench_analysis_200_boxes`). Bench on a 200-tower scene (12-core Intel, quick profile): sunhours 469→85 ms (5.5×), radiation 369→58 ms (6.3×), facesunhours 68→16 ms (4.2×), shadowstudy 19→14 ms (1.4×).
  - Step-1 deliberate skips: `minsurf`/`force_density` is Gauss–Seidel (reads already-updated neighbours — inherently sequential); `dynamic_relaxation` accumulates link forces by scatter with order-dependent float sums (a parallel gather would reassociate additions and change replayed results); LAZ decompression and the E57 reader are sequential streams; OBJ/glTF/Collada parsers are stateful (index interning/XML) — not worth the fiddle.
  - Keep ordered `collect` so output order == sequential (replay invariant); no rayon inside op-log mutation paths — analysis + import + solver inner loops only.
  - *Step 2 — wgpu compute (only if needed)*: occlusion ray casting as compute shader. Costs buffer plumbing + float-determinism care vs replay (analysis bins in op-log shield replay; keep CPU path as reference + test oracle). Deferred until step-1 profiling shows a real remaining hotspot.

## Planting — tropical/subtropical packs (planned 2026-09-03)

- [x] **M-plants-tropical** — extend the plant catalog beyond the temperate 12 (closed 2026-09-03). Catalog 12→33 (9 Caribbean, 6 Valle del Cauca, 6 Guayaquil), all real mature size/canopy/growth/deciduous; `climate_zones`+`native_regions`+`layer` tags (serde-default so legacy files still load); palms render crown-on-trunk (form "palm", 0.85 bare-trunk fraction) for low-sun shadows; `ClimateBand::from_latitude` derives Köppen band; `plant` prints a climate advisory (not an error) when a species is out of the doc's band; new `plantcatalog [region|zone]` query lists filtered species; new `miyawaki <closed-region> [density]` grows a seeded (SplitMix64 from region hash + salt, byte-stable replay) stratified native mini-forest at 3–5 saplings/m², reports species-mix %/stem count/area, warns on too-few natives, and carries the self-thinning advisory. Tests: back-compat load, Köppen bands (Guayaquil/Cali/Miami/Boston), suitability, palm-vs-tree shape, plantcatalog filtering, miyawaki determinism/count/natives/strata + replay.
  - *Schema*: species gain `climate_zones` (Köppen: Af/Am/Aw tropical, Cfa/Cwa subtropical...) + `native_regions` tags; existing 12 tagged temperate. Backward-compatible serde (untagged = temperate).
  - *Caribbean pack*: royal palm (Roystonea regia), coconut palm, ceiba (kapok), flamboyán (Delonix regia), tabebuia/roble amarillo, sea grape (Coccoloba uvifera), mango, guayacán (Guaiacum), almendro (Terminalia catappa).
  - *Valle del Cauca / Cali pack*: samán (Samanea saman — the iconic pasture giant, 25m canopy), guadua bamboo (Guadua angustifolia — clumping, structural), chiminango (Pithecellobium dulce), gualanday (Jacaranda caucana), carbonero, cachimbo (Erythrina poeppigiana).
  - *Guayaquil / coastal Ecuador pack*: guayacán (Handroanthus chrysanthus — the yellow bloom), algarrobo (Prosopis), palo santo (Bursera graveolens), ceibo (Ceiba trichistandra — bottle trunk), neem (naturalized), fernán sánchez (Triplaris cumingiana).
  - Real mature sizes/canopy/growth rates per species; palms get a dedicated canopy shape (crown-on-trunk column, not ellipsoid) — matters for shadow studies at low tropical sun angles.
  - *Climate awareness*: `location` verb already georeferences the doc — derive Köppen band from latitude (rough: |lat|<10° tropical, 10–23.5° tropical/monsoon, 23.5–35° subtropical); `plant` warns when species climate mismatches doc location ("Betula in Guayaquil — advisory: outside climate range"); `plantcatalog [region|zone]` lists filtered.
  - Deciduous flag stays (dry-season deciduous: flamboyán/ceibo drop leaves — affects seasonal shadow transparency when that lands).
  - *Miyawaki method*: `miyawaki <region-sel> [density]` — dense native mini-forest generator: picks ONLY species native to the doc's region/climate from the catalog, stratifies into 4 layers (canopy / tree / sub-tree / shrub — catalog gains a `layer` field), plants 3–5 saplings/m² in seeded-random mixed placement (never rows, neighbors differ — seed from op data so replay is stable), all at sapling age. Report: species mix %, stem count, area. Warns if catalog lacks enough native layered species for the location ("Miyawaki needs natives — N found"). Advisory line: survival thinning is part of the method (~30–50% self-thins; densities are intentional).
  - Data-driven JSON only — no code per species; packs user-extendable + LLM-authorable like plugins.

- [x] **M-plantsymbols** — 2D top-view planting symbols so plan drawings read as proper planting plans, not 3D canopies-from-above (closed 2026-09-03). Pure deterministic glyph geometry in `commands/src/landscape.rs`: `plan_symbol_segments(style, center, canopy_d)` → line-segment soup scaled to canopy diameter, lying flat on the ground; `PlanSymbol` enum with 5 standard drafting styles — **round** (24-gon circle + 8 radiating branch stubs = shade tree), **conifer** (8-point spiky star), **palm** (8 radiating frond spokes, the iconic palm glyph), **shrub** (dotted/stipple ring, also the fallback for fastigiate columns), **clump** (7 dot-crosses = bamboo/guadua). Catalog gains optional `plan_symbol` field (serde-default; derived from `form` when absent — guadua explicitly tagged `"clump"` since it is a "column" form but drafts as a cluster). `plant_object_symbol(name, positions)` is the single bridge: recovers center (XY centroid) + canopy diameter (2× max horizontal radius) + ground-z from a `plant:<id>` mesh, so every consumer draws the identical symbol at the actual planted (age-scaled) size.
  - *Where drawn*: **PDF + SVG plan exports** overlay the symbol on the 3D feature edges, gated to the **Top (plan) projection** only (a plan glyph is meaningless in elevation/iso) — SVG is always Top so always includes them, PDF gates per sheet-view direction; symbols honor the plant's layer color via the existing per-layer grouping / lineweight path. **Live viewport + pencil/NPR + headless render** overlay symbols via `scene::append_plant_symbols` behind a new `plantsymbols [on|off]` (alias `plansymbols`) toggle — 3D mesh always stays for perspective/sun analysis; symbols are added lines on top. Toggle is a deck-advertised app-verb (`VIEW_VERB_HELP`, canonical list) and forces a GPU re-upload when flipped.
  - Tests: per-style geometry counts + planar extent, linear canopy scaling, determinism, mesh-recovery of center/ground-z, legacy-catalog back-compat (no `plan_symbol` → derived from form); SVG top view emits ≥32 symbol lines on the planting layer; PDF top view draws substantially more than the same plant in front view (proves plan-gating); `append_plant_symbols` adds exactly the oak's 32 lines and is a no-op without plants; `plantsymbols` classify parsing + deck advertise/deny audit.
  - NOT done: seasonal deciduous stipple density, per-species custom symbol art (all styles are procedural), symbol in DXF export.

## GUI + usability testing (planned 2026-09-02)

- [x] **M-guitest** — automated GUI tests + automated usability audits (closed 2026-09-03):
  - [x] *Journey tests* (kittest AccessKit queries + synthetic typing/keys against the real off-screen App; GPU-`#[ignore]`d like the preview suite, `cargo test -p itsjustcad journey_ -- --ignored`): box→select→undo (asserts select is unlogged so undo reverts geometry), layer add + `layerlock` (row visible via AccessKit), autosuggest partial→popup→Tab-accept, Blocks tab lists a user `block` definition (starter param blocks keep the tab up from frame one), chat QUESTION bubble + PLAN `[x]/[ ]` checklist render (CPU kittest, runs in the normal suite).
    - Journeys found + fixed two real bugs: Tab-accept in the command line was DEAD (egui consumed Tab for focus traversal first; input now `lock_focus`es only while the popup is open) and the A+/A− text-size steppers were ~19 px tall (raised to the hit-target floor).
    - Skipped: onboarding flow + gumball drag (need real pointer capture over the 3D viewport, which previews skip headless), import/export dialogs (native rfd dialogs — not in the egui tree), Model Setup download (mock-server plumbing outweighs the assert; download.rs logic is unit-tested).
  - [x] *Snapshot suite* — pre-existed (app_dark/light, layers, dialog, panel/chat toggles, 6 chat states × 2 themes; goldens in `crates/app/tests/snapshots/`, refresh via `UPDATE_SNAPSHOTS=1 … -- --ignored`); regenerated for the A± fix. Display-mode chips not snapshotted separately: the chips live over the 3D viewport pass that headless previews skip.
  - [x] *Usability audits as tests*: AccessKit-tree walk asserts every interactive widget ≥ `Spacing::HIT_TARGET` height; keyboard-only Tab traversal never dead-ends and cycles back to the start; native-menu accelerators unique per action per style (same action twice on one shortcut allowed — Settings/Model Setup on ⌘,); reduce_motion busy indicator is static (no repaint loop) while the spinner demonstrably loops. WCAG ≥4.5:1 for both theme palettes already covered in theme.rs (`mode_palettes_meet_wcag_aa` + viewport-tag/status tests). Disabled-don't-hide already covered by menu.rs `selection_edit_items_disabled_when_empty`.
  - [x] *Headless `--shot` theme*: already honors `ITSJUSTCAD_THEME=dark|light` (headless.rs `resolve_theme` + env round-trip tests) — the "hardcodes dark" gap was closed earlier. Full GUI-chrome capture from `--shot` deliberately NOT added: it would drag the eframe/kittest test harness into the shipping binary; the kittest preview tests already produce full-chrome PNGs in both themes.
  - Snapshot/GPU tests stay `#[ignore]`d (Metal-only) while logic journeys (chat render, reduce-motion, shortcut audit) run everywhere; human usability testing stays manual.

## intemfit — lot subdivision (planned 2026-09-03 — NOT started; plan only)

- [ ] **M-intemfit** — parametric site planning (roads → blocks → lots), CityEngine/TestFit-style, as a native ItsJustCAD Rust feature (target decided 2026-09-03: NOT a Rhino plugin, NOT a JSON macro). Full build plan in **`docs/intemfit-plan.md`** (grounded in Manuel's questionnaire — ask before inventing requirements). Shape:
  - New pure-Rust `crates/subdivision` (no egui/doc deps): `Polygon2d` + `i_overlay` bridge (offset/boolean — the Clipper2 role; **Phase-1 blocker: confirm i_overlay offset quality + AGPLv3 fit**), `OrientedBox`, recursive-OBB / offset / straight-skeleton subdivision, street generators (orthogonal/skewed/organic/culdesac), lot rules (width-mix packing, depth, corner, flag, front/alley loading, sliver merge), setbacks + buildable envelope, open-space placement, footprints/roofs, yield reporting.
  - `lot*` verbs in crates/commands (deck-callable, GBNF, logged + replay-stable, seeded-deterministic); results bake to `lots`/`roads`/`blocks` layers; yield via the `report` plane; viewport preview before commit. NO Rhino toolbar/.rui/multi-target — those are dropped.
  - Phase order: 1 geom foundation → 2 verb+settings+preview → **3 recursive-OBB (first shippable, send to Manuel)** → 4 offset → 5 roads+block-extraction+street-tagging → 6 lot rules → 7 skeleton → 8 setbacks → 9 open space → 10 buildings → 11 yield → 12 polish. Do NOT attempt 10–12 in the first release.
  - Owner scope beyond Manuel (Hector, 2026-09-04): radial/circular + hexagonal + **Voronoi** street generators (Phase 5b — non-rectilinear; Voronoi = dual of our Bowyer-Watson Delaunay, no new dep) + a `loose` lot mode (irregularity unlocks past 0.4 to 1.0, routes to the organic subdivider) + opt-in blind %-open-space (`lotopenspace reserve=<pct>`, default off — feature placement stays default; reserved blocks netted out of yield). Still ruled out: whole-site no-subdivision mode. Open questions before Phase 5/6: width-mix product list, alley dims, "day one" blank (Q14), flag-lot area accounting.
  - Blockers to clear before coding: confirm i_overlay offset; obtain the referenced Python prototype (`/prototype/python`, not yet in repo) to port from; resolve Manuel's open questions.

## Compliance plugins (engine SHIPPED 2026-09-03; packs still planned)

Shared foundation first, then two rule packs riding it. Advisory only — every
report carries "advisory pre-check, not a code review; verify with a licensed
professional / AHJ". Rule packs are data (JSON), never hardcoded, so editions
update without code and the deck LLM can read/extend them.

- [x] **M-checkengine** — SHIPPED 2026-09-03: declarative compliance-check engine (`crates/commands/src/checkengine.rs`).
  - Rule = pure JSON: id, code_ref, severity (error/warn/info), target query (kinds curve/mesh/block/door/stair/slab/wall/beam/column + layer + name_contains + story band), geometric predicate + threshold, message. Pack = named rule list, hard-validated on parse (malformed JSON, unknown kinds/severities, empty/duplicate ids, non-positive thresholds all rejected).
  - Engine emits `ComplianceReport` (per-rule verdict pass/fail/warn/info + object ids + locations + measured vs required + checked count), stored in `Document::compliance_reports` and served by the existing `report` verb (`report codecheck`; bare `report` includes it) — exact `AnalysisReport` plumbing, serde-back-compat for old files. The advisory disclaimer lives IN the report context so every rendering carries it.
  - Probes shipped (pure fns, each analytically tested): ramp/path slope (same math as the sitepath 1:12 advisory), sampled perpendicular clear width (BVH ray-casts at 1 m probe height), door clear width (param-block `width`×scale, else block-def plan extent — honest limits documented), riser extraction from stepped meshes (up-face z-level clustering), headroom (vertical ray-cast), turning-circle fit (simplified planar 60" disc in a person-height band), count-per-story (aabb-bottom banding). NOT shipped: travel-distance along route (deferred to the packs that need it).
  - Verbs: `codecheck <pack> [story]` — logged; the resolved rules AND marker ids are embedded into the op on first exec (CutFill precedent) so replay is disk-free and bit-stable; severity-colored failure markers (red/orange/blue circles) on the 'compliance' layer, undo-clean. `checkrules list|load <path>` manage packs (`~/.config/itsjustcad/checks/<name>.checks.json`; `codecheck` auto-resolves that dir on a miss). Registry-listed → deck-callable, GBNF-covered, menu/palette-visible.
  - Deck prompt section (CODECHECK_HELP) teaches codecheck → `report codecheck` → critique grounded in rule ids, lists every check-kind so the LLM can author packs (draft JSON → user saves → `checkrules load`), and hard-requires the advisory disclaimer ("never claim a design complies"). Prompt-completeness test pins it.
  - Embedded demo pack `assets/checks-demo.json` (6 rules: ramp ≤1:12 error, door ≥32in error, riser ≤7in error, headroom ≥80in warn, route width ≥36in warn, guard-drop >30in info) proves the engine end-to-end against a built violating scene. ~25 new tests (schema, probes, engine e2e, report serving, replay stability, undo, story filter, prompt completeness).
- [x] **M-ibc** — SHIPPED 2026-09-03: IBC 2021 rule pack (`assets/checks-ibc2021.json`, 11 rules, embedded + registered alongside `demo`) riding the M-checkengine engine, data-driven so a 2024 edition swaps in.
  - Room semantics: `room <closed-curve> <occupancy>` verb tags a closed-curve region with an IBC use group (assembly|business|residential|mercantile|educational|storage|institutional) + computed shoelace area, stored as `Document::rooms` (`Room` type: name, occupancy, area, boundary; serde-back-compat, undo/replay-stable via `Inverse::RoomList`); `rooms` lists them. Registry + parse + deck-callable + GBNF-covered.
  - New backward-compatible `CheckKind` variants (serde-tagged): `occupant_load` (room area ÷ IBC Table 1004.5 factor — factors are DATA in the pack JSON), `exit_count` (occupant load → required exits via IBC 1006.3.2 `[max_load, exits]` bands, also pack data; exits are name-matched "exit" objects within room reach), `travel_distance` (room centroid → nearest exit, HONEST straight-line + noted, IBC 1017.2 250 ft sprinklered), `tread_depth` (stair plan run ÷ riser count, IBC 1011.5.2).
  - Pack rules: riser ≤7in (1011.5.2), tread ≥11in (1011.5.2), stair headroom ≥80in (1011.3), stair width ≥44in (1011.2), corridor ≥44in (1020.2), door ≥32in clear (1010.1.1), guard-drop >30in (1015.2-3, name-match honesty note), habitable ceiling ≥90in (1208.2), occupant load (Table 1004.5), exit count (1006.3.2), travel distance ≤250ft (1017.2). Every message carries an IBC section ref + advisory disclaimer; guard/ceiling are name-matched only, travel distance is straight-line — noted, kept honest.
  - Prompt: `CODECHECK_HELP` extended with the ibc pack + room-tagging egress flow + the 4 new check kinds; completeness test green. ~10 new tests (room area/undo/replay, occupant-load 100m²@150gross→8, exit-count thresholds, tread-depth synthetic stair, travel-distance known layout, full pack e2e, pack validates).
  - Honest limits: handrail/guard/ceiling checks are name-match only (no geometric guard/handrail modeling); travel distance is straight-line, NOT the routed egress path; exit-count uses a proximity heuristic (exits within the room's half-diagonal), not door-graph egress. All disclosed in messages + module/prompt docs.
- [x] **M-ada** — SHIPPED 2026-09-03: ADA 2010 Standards / ICC A117.1 rule pack (`assets/checks-ada.json`, 12 rules, embedded + registered alongside `demo`/`ibc2021`) riding the M-checkengine engine, data-driven.
  - New backward-compatible `CheckKind` variants (serde-tagged): `ramp_landing` (flat-segment landing detection along a ramp centerline — flags a run rising >`max_rise`/30 in without a ≥`landing_len`/60 in near-flat landing, `missing_landings` probe, ADA 405.7), `threshold_height` (door step ≤`max` from the door block `threshold` param, else honestly flagged "not modelably detectable", ADA 404.2.5), `max_cross_slope` (perpendicular grade of a ramp MESH surface via up-facing face normals, run/cross axes inferred from AABB, ADA 403.3/405.3), `count_ratio` (accessible count vs total via ADA Table 208.2 `[max_total, min_accessible]` bands — DATA, name-match heuristic). Each has an analytic unit test (ramp with/without landing, threshold param/none, cross-slope flat vs tilted plate, ratio bands).
  - Pack rules: ramp running slope ≤1:12 (405.2), ramp cross slope ≤1:48 (405.3), ramp landings (405.7), route clear width ≥36 in + 32 in pinch (403.5.1), door clear width ≥32 in (404.2.3), threshold ≤½ in (404.2.5), 60 in turning space (304.3.1), clear floor space 30 in (305.3, only width probed), handrail height ≥34 in (505.4, name-matched), accessible parking count Table 208.2 (406/curb slope 406.1). Every message carries an ADA section ref + advisory disclaimer.
  - Prompt: `CODECHECK_HELP` extended with the ada pack + accessible-route flow (name ramp-1/route-1/turn-1/parking-N) + the 4 new check kinds; completeness test green. Tests: 5 new probe/pack tests in checkengine + ADA pack e2e on a violating scene (ramp passes running slope but fails landings, narrow door fails clear width, threshold flagged) with replay stability.
  - SKIPPED (honest, documented in module + pack + prompt): reach ranges and door maneuvering clearances — the model carries NO fixture-height or door-swing geometry, so any number would be fabricated; restroom fixture clearances and stair handrail *extensions* likewise omitted (no fixture/handrail-end geometry). Threshold is param-only (no small-step mesh geometry); cross slope needs an actual ramp mesh (a drawn centerline carries none); handrail height is name-matched. All limits disclosed.
  - Reuses M-landscape ramp/slope math (`steep_segments`) and check-engine probes; site + building both.

## Interop (M7 remainder) — COMPLETE ✅ (audited + closed 2026-09-02)

Audit found nearly all of M7 had already shipped; the two real gaps (LAZ,
.3dm export) closed this session.

- [x] **IFC** — import (IFC4/IFC2x3, meshes → 'ifc' layer, semantic variant for M-bim-import) + export (IFC4). *(already shipped)*
- [x] **3DM** — pure-Rust openNURBS reader (meshes/lines/polylines/NURBS curves with names + layers; breps skipped) — no opt-in download needed. Export shipped 25d004a: the spec-conformant V5 writer promoted from test-only to `export .3dm`.
- [x] **SVG / CSV export** — shipped earlier (svg.rs, csv.rs, wired to dialog + registry).
- [x] **Point clouds** — LAS 1.2–1.4 (hand-rolled) + E57 (e57 crate) already in; **LAZ decompression added b5223c4** via laz-rs (Apache-2.0), batch-decompressed so decimation to ≤200k never materializes the cloud.
- [x] **Mesh import** — OBJ/STL/glTF/GLB/Collada .dae all shipped (mesh_import.rs, hand-rolled parsers with size caps).
- [x] STEP AP242 import + faceted export — implemented via OCCT, but **gated behind the opt-in 'kernel-occt' feature** (M-kernel); default builds report a clear "needs the exact-BREP tier" error.
- DWG / SKP / RVT: bridge via open exchange only (no proprietary readers — license clean).

## Long-term / research (audited 2026-09-02 — most had ALREADY shipped)

- [x] **M-kernel** — DONE as designed: opt-in OCCT tier exists (`kernel-occt` crate behind the `kernel-occt` feature; STEP + exact booleans route through it, default builds report the clear "needs the exact-BREP tier" error). Mesh kernel stays default. No further work planned.
- [x] **M-plugins** — DONE (was already shipped; verified): declarative JSON macros (params + command-template body, NO native code), persisted to `~/.config/itsjustcad/plugins/`, loaded at startup, `plugin list|reload|save|define|delete` verbs, menu integration, path-traversal + prompt-injection hardening, and the deck system prompt teaches the LLM to author macros mid-chat via `plugin define {json}` (17 tests).
- [x] **M-nurbs** — COMPLETE this session: interpolation (`interpcurve`) + control-point editing (`setpoint` + draggable UI) were already in; **added knot insertion** (`insertknot`, Boehm on homogeneous coords, shape-exact for rational curves) and **curvature graph** (`curvature` — comb hairs + tip curve on 'analysis', Menger curvature, reports max κ / min radius). Not done (niche): degree elevation; NURBS-output rebuild (rebuild emits polylines).
- [x] **M-solids2** — COMPLETE this session: sweep2, rail revolve and variable-radius pipe were already in; **added loft with guide curves** (`loft <profiles> guides <sel>` — skin bows through open guides, cosine falloff) and **blend surface** (`blend <a> <b> [bulge]` — Hermite-eased sheet, bulge 1 = ruled).
- [x] **M-constraints** — CORE SHIPPED 2026-09-02 (audit: was truly not started). New pure-std `crates/constraints`: 2D parametric sketch solver, Newton–Raphson with Levenberg-Marquardt damping (SolveSpace approach), numeric Jacobian, rank diagnostics (DOF count, redundant-constraint blame, per-constraint conflict list); 19 constraint kinds; 32 tests incl. 25 scrambled-start convergence runs. Command layer: `constrain <kind> <sel> [sel] [value]` (14 kinds — coincident/horizontal/vertical/distance/length/angle/parallel/perpendicular/equal/radius/fixed/tangent/midpoint/on; polymorphic by target type, nearest-endpoint resolution latched into the stored constraint, solves immediately), `solveconstraints`, `constraints list|delete <n>|clear`. Undo/redo-able, op-log replay-stable, deck-callable, solver status (solved / N DOF / redundant / conflicts) in the command output. Works on lines + circles/arcs. Remaining (niche until a sketch-mode UI exists): constraints on polyline vertices / NURBS control points, `symmetric` via command line (solver supports it), constraint glyphs in the viewport, drag-with-constraints.
- [x] **M-site / M-basemap** — mostly DONE (was already shipped; verified): `terrain <csv|geojson>` from contours, GeoJSON/OSM import, OSM + keyless-satellite slippy-tile basemaps (offline-first, transient, never op-logged), `location` georeferencing.
- [x] **M-assets** — COMPLETE this session: block library (`blocklib/blockload/blocksave` + 6 seeded starters), 7 drafting hatches and Hershey vector drafting font were already in; **added the ANSI standard hatch set** (`hatch <sel> ansi31..ansi38 [spacing]` — iron/steel/bronze/plastic/fire-brick/marble/lead/aluminum; renders in viewport + PDF, scales with the object, replay-stable). Not done (niche): TrueType drafting fonts, user-defined hatch rules.
- [x] **M-dynblocks** — DONE (was already shipped; verified): `pblock <name> [param=default ...] : templates` parametric blocks, `insert ... param=value` overrides, `param <sel> key=value` re-derives geometry. No constraint-driven grips (see M-constraints).
- [x] **M-cloudfiles** — COMPLETE this session: atomic saves (write-temp + fsync + rename) and crash journal were already in; **added external-change detection** (mtime/size watch, throttled 2 s poll, Reload-vs-keep-mine modal, deleted-file notice) and **conflict-copy awareness** (Dropbox "conflicted copy" + numbered-duplicate siblings warned at open/save).
- [x] **M-histedit / M-options** — DONE (was already shipped; verified): `amend <step> <command>` op-log rewriting with full replay, `option save/list/switch/delete` design-option branches persisted in the file format. Not done: arbitrary rebase-style history surgery (amend-only by design).
- [ ] **M-i18n / M-docs** — NOT STARTED (audited: no i18n infra, all UI strings hardcoded). Localization (Spanish first), tutorials + sample files + website. Real remaining work.

## Pre-release

- [ ] **PRE-PUSH PASS** — README end-user Download & Install section; 6 value-prop screenshots (massing, pencil section, sun study, dimensioned sheet→PDF, deck-from-prompt, Rhino-skinned UI); SECURITY.md present + audited (M-secreview; no separate security-review doc exists to `git rm`); fix repo URL owner; push main + tag v0.1.
- [x] **M-secreview / M-codereview** — untrusted-input hardening pass (2026-09-03; rerun-before-each-release). AUDITED every parser + JSON loader for OOM/overflow/index-panic/traversal/prompt-injection.
  - **Fixed** (regression test per fix, all in mesh_import.rs): binary-STL `84 + 50*tri_count` unchecked multiply (wrapped past the length gate → parallel over-read) → checked_mul/add; GLB `data_start + chunk_len` wrap past the bounds check → checked_add; Collada `<triangles>`/`<polylist>` file-supplied input-offset > stride + vcount·stride usize overflow (panic on index) → `.get()` guards + checked arithmetic. Commit 5129bd8.
  - **Verified already-safe** (audit surfaced these; confirmed hardened, no change needed): point clouds (LAS/LAZ/E57) — `MAX_POINTS` decimation, `record_length`/offset validated vs file len, LAZ tolerates truncated tail; 3DM (rhino3dm) — `cap_hint` clamps allocs to remaining buffer, NURBS `order`/`cv_count` range-validated *before* the knot arithmetic, no trusted deflate path; GeoJSON/Overpass — `serde_json` depth-limited parse returns clean Err; JSON loaders (plugin/block/checkpack) — every name→path flow validated (rejects `/` `\` `.`/`..`) *before* `.join`, with existing traversal tests; **prompt injection** — SECURITY.md's sanitization claim VERIFIED true: single choke point `digest::sanitize_name` strips backticks/control/newlines, collapses whitespace, caps to 64 chars, wraps in «» guillemets; imported names cannot forge a `draft` fence.
  - **Accepted low** (documented in SECURITY.md, no fix): SD `ControlImages::from_prefix` reads `<prefix>_depth.png` from an app-generated prefix in a private dir (fixed suffix + internal prefix → not attacker-reachable); config files read whole into memory (user's own dir, small by design).
  - **Docs:** SECURITY.md rewritten — supported versions, report to htarrido@pm.me / GH advisory, expanded threat model, "what is hardened", re-run checklist. No stale security-review doc existed to `git rm` (only PRE-PUSH PASS references one).
  - **Remaining / future rerun:** DXF and EPW parsers use safe split/parse patterns (no unbounded alloc or index panic found) but have no dedicated adversarial corpus yet; a broader fuzz harness (arbitrary/cargo-fuzz over each importer) is the natural next step.
- [ ] **M-sign** *(deprioritized)* — Apple + Windows code-signing certs to kill first-launch warnings. Only when marketing to non-technical architects at scale.

## Out of scope (this repo)

- iOS/iPadOS/mobile app — separate private/paid repo consuming the shared egui-free Rust core.
- Web/WASM viewer — dropped.
